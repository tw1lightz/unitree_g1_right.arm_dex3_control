---
phase: 4
plan: 04-01
title: "Right-arm index constants and minimal hand-publish removal"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/unitree_g1_dex3_stack-main/src/joint_trajectory_executor.cpp
autonomous: true
requirements:
  - INTG-02
---

<objective>
Lay the foundation for Phase 4's narrowing work without changing runtime behavior of the publish loops. Add file-private right-arm joint index/name constants used by Plans 04-02 and 04-03, and apply CONTEXT.md D-01 "minimal removal" of the two `hand_cmd_pub->publish(hand_cmd)` calls. All other infrastructure (publishers, members, ctor `create_publisher`, `is_left_hand` detection block, `sleep_for(1s)` after the now-deleted hand-open publish) stays inert per D-01/D-02.
</objective>

<must_haves>
## Goal-backward verification

<truths>
- File-private `kRightArmJointIndices` array with 7 `JointIndex` enum values exists at the top of `joint_trajectory_executor.cpp` (anonymous namespace).
- File-private `kRightArmJointNames` array with 7 `std::string_view` values exists adjacent to `kRightArmJointIndices`, names matching `joint_name_to_index` keys exactly.
- Both arrays are declared in the same order (slot k of names corresponds to slot k of indices).
- Line 182 (`hand_cmd_pub->publish(hand_cmd);` for hand-open) is deleted.
- Line 270 (`hand_cmd_pub->publish(hand_cmd);` for hand-close) is deleted.
- The `hand_cmd.data = false;` (line 181) and `hand_cmd.data = true;` (line 269) assignment statements are kept (D-01 minimal-removal stance — easier future re-enable).
- The `sleep_for(1s)` at line 184 is kept verbatim (D-01).
- `left_hand_pub_` / `right_hand_pub_` declarations (L134-135), constructor `create_publisher<Bool>` calls (L58-59), and `is_left_hand` detection block (L158-168) are unchanged (D-01).
- No `[[maybe_unused]]` attribute is added (D-02). Unused-variable warnings on `hand_cmd_pub` are accepted.
- The three publish loops (waypoint L193-232, hold L272-294, ramp L307-340) and the `standing_pose` / `trajectory_endpoint` snapshot logic are untouched.
- `colcon build --packages-select unitree_g1_dex3_stack` succeeds with exit code 0.
</truths>
</must_haves>

<tasks>

## Task 1: Add file-private right-arm index and name arrays

<read_first>
- src/unitree_g1_dex3_stack-main/src/joint_trajectory_executor.cpp (lines 1-46 for include block + helper struct, to find the right insertion point)
- src/unitree_g1_dex3_stack-main/include/g1_dex3_joint_defs.hpp (lines 17-57 for `JointIndex` enum and lines 78-108 for `joint_name_to_index` keys; the 7 right-arm enum values are `kRightShoulderPitch`..`kRightWristYaw`, idx 22-28)
</read_first>

<action>
1. Add `#include <array>` and `#include <string_view>` to the include block at the top of `joint_trajectory_executor.cpp` if either is missing. Place adjacent to the existing `<vector>` / `<map>` / `<string>` includes.
2. Insert an anonymous namespace block (`namespace { ... }`) immediately after the `JointLimits` struct definition (around current line 46) and before `class JointTrajectoryExecutor`. Internal linkage is required so the constants are file-private.
3. Inside the anonymous namespace, declare two `static constexpr std::array` constants:
   - `kRightArmJointIndices` — `std::array<JointIndex, 7>` containing the 7 right-arm enum values from `g1_dex3_joint_defs.hpp` in the order: `kRightShoulderPitch, kRightShoulderRoll, kRightShoulderYaw, kRightElbow, kRightWristRoll, kRightWristPitch, kRightWristYaw`.
   - `kRightArmJointNames` — `std::array<std::string_view, 7>` containing the 7 right-arm joint name strings used in `joint_name_to_index`, in the SAME order: `right_shoulder_pitch_joint, right_shoulder_roll_joint, right_shoulder_yaw_joint, right_elbow_joint, right_wrist_roll_joint, right_wrist_pitch_joint, right_wrist_yaw_joint`.
4. Above both arrays, add a single comment stating that they are consumed by Plan 04-02 (validation) and Plan 04-03 (publish-loop narrowing), and that indices and names must stay in sync (same order).
5. The two arrays must be `constexpr`. Use `std::string_view` (not `std::string`) for the names array so it stays `constexpr`.
</action>

<acceptance_criteria>
- `grep -c 'kRightArmJointIndices'` in `joint_trajectory_executor.cpp` is exactly 1 (definition only at this point).
- `grep -c 'kRightArmJointNames'` in `joint_trajectory_executor.cpp` is exactly 1.
- The seven enum values in `kRightArmJointIndices` are in the exact order: `kRightShoulderPitch, kRightShoulderRoll, kRightShoulderYaw, kRightElbow, kRightWristRoll, kRightWristPitch, kRightWristYaw`.
- The seven strings in `kRightArmJointNames` are in the exact same order: `"right_shoulder_pitch_joint"`, `"right_shoulder_roll_joint"`, `"right_shoulder_yaw_joint"`, `"right_elbow_joint"`, `"right_wrist_roll_joint"`, `"right_wrist_pitch_joint"`, `"right_wrist_yaw_joint"`.
- Both arrays are inside an anonymous namespace (`namespace { ... }`).
- `#include <array>` and `#include <string_view>` are present in the include block.
</acceptance_criteria>

## Task 2: Delete the two hand_cmd_pub publish calls

<read_first>
- src/unitree_g1_dex3_stack-main/src/joint_trajectory_executor.cpp (lines 178-186 for the hand-open block; lines 266-272 for the hand-close block)
</read_first>

<action>
1. Delete the single statement `hand_cmd_pub->publish(hand_cmd);` at the hand-open site (current line 182, inside `trajectoryCallback`, immediately after the `hand_cmd.data = false;` assignment). Delete the entire line including its trailing `// Open hand command` comment.
2. Delete the single statement `hand_cmd_pub->publish(hand_cmd);` at the hand-close site (current line 270, immediately after the `hand_cmd.data = true;` assignment, just before the 1 s hold-publish loop block). This line has no trailing comment.
3. Above each deleted location, leave a one-line comment marking the D-01 minimal-removal decision: at the hand-open site, note that the publish is deleted and the `sleep_for(1s)` below is kept as an inert settling delay; at the hand-close site, note that the publish is deleted and the `hand_cmd.data = true;` assignment is kept for future DEX3 re-enable.
4. Preserve verbatim: line 180 (`std_msgs::msg::Bool hand_cmd;`), line 181 (`hand_cmd.data = false;` plus its `// Open hand command` comment), line 184 (`rclcpp::sleep_for(1s);` plus its `// Wait for the hand to open` comment), line 185 (the `RCLCPP_INFO(... "Hand opened fully, ...")`), and line 269 (`hand_cmd.data = true;`).
5. Do NOT modify: the `left_hand_pub_` / `right_hand_pub_` member declarations (L134-135), the `create_publisher<std_msgs::msg::Bool>` calls in the constructor (L58-59), or the `is_left_hand` detection block at L158-168 (the substring search, the resulting bool, the `RCLCPP_INFO(... "Executing trajectory for %s hand", ...)`, and the `hand_cmd_pub` selection). All three remain inert per D-01.
6. Do NOT add `[[maybe_unused]]` to the now-unused `hand_cmd_pub` local variable (D-02). If the build emits an unused-variable warning, leave it.
</action>

<acceptance_criteria>
- `grep -c 'hand_cmd_pub->publish'` in `joint_trajectory_executor.cpp` returns `0`.
- `grep -c 'rclcpp::sleep_for(1s)'` returns `1` (the inert-settling delay is preserved).
- `grep -c 'hand_cmd.data = false'` returns `1` (assignment kept).
- `grep -c 'hand_cmd.data = true'` returns `1` (assignment kept).
- `grep -c 'left_hand_pub_'` returns the same count as before this task (publishers preserved).
- `grep -c 'right_hand_pub_'` returns the same count as before this task.
- `grep -c 'is_left_hand = '` returns `1` (detection preserved).
- Two `// Plan 04-01 (D-01 minimal removal):` comments are present in the modified file.
</acceptance_criteria>

## Task 3: Build verification

<read_first>
- src/unitree_g1_dex3_stack-main/CMakeLists.txt (no changes expected — just a sanity scan to confirm executor target unchanged)
</read_first>

<action>
Run `colcon build --packages-select unitree_g1_dex3_stack` from the workspace root (`src/` parent). Confirm exit code 0 and that the build log shows the executor target was rebuilt.

If the build fails with an unused-variable warning on `hand_cmd_pub` and `-Werror` is set in CMake, do NOT add `[[maybe_unused]]`. Instead, surface this back to the user as a follow-up question — the project's existing warning policy is the source of truth.
</action>

<acceptance_criteria>
- `colcon build --packages-select unitree_g1_dex3_stack` exits with code 0.
- The build log contains a line referencing `joint_trajectory_executor.cpp` (the file was rebuilt).
- No new errors or warnings introduced beyond the expected unused-variable warning on `hand_cmd_pub` (which is allowed per D-02).
</acceptance_criteria>

</tasks>

<verification>
## Verification Steps

1. `colcon build --packages-select unitree_g1_dex3_stack` exits 0.
2. `grep -c 'hand_cmd_pub->publish' src/unitree_g1_dex3_stack-main/src/joint_trajectory_executor.cpp` returns `0`.
3. `grep -c 'kRightArmJointIndices' src/unitree_g1_dex3_stack-main/src/joint_trajectory_executor.cpp` returns `1`.
4. `grep -c 'kRightArmJointNames' src/unitree_g1_dex3_stack-main/src/joint_trajectory_executor.cpp` returns `1`.
5. `grep -c 'rclcpp::sleep_for(1s)' src/unitree_g1_dex3_stack-main/src/joint_trajectory_executor.cpp` returns `1`.
6. Line count delta: `wc -l` shows ~+15 lines (two array definitions + 2 includes + 2 comments) and ~−2 lines (deleted publish calls). Net within +5 to +20.
7. Visual diff review: confirm only the four touch points (anonymous namespace block, two deleted lines + their new comments).
</verification>

<threat_model>
## Security / Safety Threats Considered

- **Inert hand publishers as dormant code**: keeping `hand_cmd_pub` un-`publish()`ed is functionally safe — `std_msgs::msg::Bool` publishers without subscribers are no-ops. No new attack surface.
- **`[[maybe_unused]]` decision**: deliberately omitted (D-02). Risk: -Werror builds could fail. Mitigation: Task 3 surfaces this for user follow-up rather than silently suppressing.
- **Unchanged publish loops**: this plan does not touch the trajectory/hold/ramp loops, so all existing safety guarantees (Plan 01-04..01-12 compound-bug fixes, master-switch ramp, standing_pose snapshot) carry forward unchanged.
- **No firmware behavior change**: arm_sdk semantics are unaffected — Phase 4's risky firmware-coupling change happens in Plan 04-03, not here.

No HIGH severity threats. LOW severity unused-variable warning is the only concern; explicit user-surface path for it.
</threat_model>

<success_criteria>
- `kRightArmJointIndices` and `kRightArmJointNames` available in file scope for Plans 04-02 and 04-03 to consume.
- Hand publish calls removed per D-01 minimal removal.
- Build succeeds.
- Runtime behavior of trajectory execution is identical to pre-Plan-04-01 except the two now-missing hand publishes (no DEX3 hand opens or closes during a trajectory).
</success_criteria>
