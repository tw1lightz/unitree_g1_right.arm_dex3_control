---
phase: 4
plan: 04-03
title: "Document D-06 Option A decision; update ROADMAP criteria; no publish-loop code changes"
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - src/unitree_g1_dex3_stack-main/src/joint_trajectory_executor.cpp
  - .planning/ROADMAP.md
  - .planning/phases/04-right-arm-only-executor/04-CONTEXT.md
autonomous: true
requirements:
  - INTG-02
---

<objective>
Record the D-06 Option A decision (user choice 2026-05-14): the three publish loops keep the existing 28-joint `mode=1, q=latest_joint_positions_[idx], kp=60, kd=1.5` fill unchanged. Non-right-arm 21 joints are locked at their current positions rather than left default-constructed, matching `xr_teleoperate/free_arm_demo.py` canonical coexistence pattern and 1+ year of standing-mode evidence. The trajectory column override introduced in Plan 04-02 already handles right-arm q values correctly — no further code surgery is needed. Tasks in this plan are: (1) add an explanatory D-06 Option A comment above each of the three 28-joint fill loops in source; (2) update ROADMAP.md Phase 4 success criteria 1-2 to reflect the 28-joint lock reality; (3) record the Option A choice in CONTEXT.md D-06.
</objective>

<must_haves>
## Goal-backward verification

<truths>
- The three publish loops (waypoint, hold, ramp) preserve the D-06 Option A pattern verbatim: `for (const auto& pair : joint_name_to_index)` iterates all 28 body joints with `mode=1, kp=60.0f, kd=1.5f, dq=0.f, tau=0.f`; `q = latest_joint_positions_[idx]` (with `0.0f` fallback). Nothing in these loops is deleted or restructured.
- The trajectory column override from Plan 04-02 (`for (size_t k = 0; k < right_arm_columns_in_msg.size(); ++k)` writing `cmd_msg.motor_cmd[target_index].q = target_position`) follows immediately after the 28-joint fill in the waypoint loop, overriding the right-arm slots with trajectory-commanded positions. This is the complete right-arm control path.
- Above each of the three 28-joint fill loops, a one-line D-06 Option A comment is added explaining the keep-all-28 decision.
- ROADMAP.md Phase 4 success criterion 1 is updated from "LowCmd only populates motor commands for right arm joint indices" to reflect "28 joints locked with kp=60; right-arm q driven by trajectory".
- ROADMAP.md Phase 4 success criterion 2 is updated from "All non-right-arm joint command entries left at zero/untouched" to reflect "non-right-arm joints locked at latest_joint_positions_ with kp=60".
- CONTEXT.md D-06 decision body is updated to mark Option A as the resolved choice with rationale (user preference 2026-05-14; consistent with free_arm_demo.py and 1+ year standing evidence; no bench-test gate required).
- Master switch, timing constants (`hold_steps=250`, `interp_steps=750`), `standing_pose`/`trajectory_endpoint`/`ramp_start_positions` snapshot logic, and `ramp_start_positions = trajectory_endpoint` assignment are unchanged.
- `colcon build --packages-select unitree_g1_dex3_stack` succeeds with exit 0.
</truths>
</must_haves>

<tasks>

## Task 1: Add D-06 Option A comments to the three 28-joint fill loops

<read_first>
- src/unitree_g1_dex3_stack-main/src/joint_trajectory_executor.cpp (lines 193-232 waypoint loop body; lines 272-294 hold loop body; lines 307-340 ramp loop body — specifically the `for (const auto& pair : joint_name_to_index)` header line in each loop)
</read_first>

<action>
1. Locate the `for (const auto& pair : joint_name_to_index)` loop header in the **waypoint loop body** (current ~L199). Immediately above this line, insert a one-line comment: `// Plan 04-03 D-06 Option A: all 28 body joints locked with kp=60; non-right-arm joints held at latest_joint_positions_. Trajectory column override below drives right-arm q. Matches free_arm_demo.py coexistence pattern.`
2. Locate the `for (const auto& pair : joint_name_to_index)` loop header in the **hold loop body** (current ~L280). Immediately above it, insert: `// Plan 04-03 D-06 Option A: all 28 body joints locked with kp=60 at trajectory end-point.`
3. Locate the `for (const auto& pair : joint_name_to_index)` loop header in the **ramp loop body** (current ~L315). Immediately above it, insert: `// Plan 04-03 D-06 Option A: all 28 body joints locked with kp=60; q interpolates to standing_pose over 3 s.`
4. Do NOT touch any loop body. The three `for (const auto& pair : joint_name_to_index)` loops and all their inner statements stay byte-identical.
</action>

<acceptance_criteria>
- `grep -c 'Plan 04-03 D-06 Option A'` in `joint_trajectory_executor.cpp` returns exactly `3`.
- `grep -c 'for (const auto& pair : joint_name_to_index)'` returns `4` (the `lowstateCallback` body plus the three publish loops — unchanged count from post-Plan 04-02).
- Line count delta (`wc -l`) is exactly `+3` (three comment lines added, nothing removed).
- Visual diff shows only three inserted comment lines, no other changes.
</acceptance_criteria>

## Task 2: Update ROADMAP.md Phase 4 success criteria 1-2

<read_first>
- .planning/ROADMAP.md (lines 64-80 — Phase 4 Goal, Requirements, Success Criteria block)
</read_first>

<action>
1. Replace success criterion 1 (`LowCmd only populates motor commands for right arm joint indices (kRightShoulderPitch through kRightWristYaw)`) with: `LowCmd populates all 28 body motor command slots locked with kp=60/kd=1.5; right arm joint positions driven by trajectory values (kRightShoulderPitch through kRightWristYaw); other joints locked at latest measured positions.`
2. Replace success criterion 2 (`All non-right-arm joint command entries left at zero/untouched`) with: `Non-right-arm joint motor commands locked at latest_joint_positions_ with kp=60 (Option A: consistent with free_arm_demo.py canonical coexistence pattern; unchanged from Plan 01-10 baseline).`
3. Preserve criteria 3, 4, 5 exactly (hand removal, running-mode balance, no interference).
</action>

<acceptance_criteria>
- `grep -c 'LowCmd only populates motor commands for right arm' .planning/ROADMAP.md` returns `0` (old criterion removed).
- `grep -c 'All non-right-arm joint command entries left at zero' .planning/ROADMAP.md` returns `0` (old criterion removed).
- `grep -c 'LowCmd populates all 28 body motor command slots' .planning/ROADMAP.md` returns `1` (new criterion 1 present).
- `grep -c 'Non-right-arm joint motor commands locked at latest_joint_positions_' .planning/ROADMAP.md` returns `1` (new criterion 2 present).
- Criteria 3, 4, 5 remain unchanged (grep for `Hand open/close commands removed`, `Running mode maintains balance`, `No observable interference`).
</acceptance_criteria>

## Task 3: Update CONTEXT.md D-06 to record Option A resolution

<read_first>
- .planning/phases/04-right-arm-only-executor/04-CONTEXT.md (the D-06 decision entry under `### Trajectory Side Validation` or equivalent `<decisions>` block)
</read_first>

<action>
1. Locate the D-06 decision block in CONTEXT.md (the one about master-switch semantics under right-arm-only writes).
2. Append to the end of the D-06 text body (after the existing content, within the same decision entry): `[RESOLVED 2026-05-14: Option A chosen — preserve 28-joint fill with kp=60 unchanged. Rationale: consistent with xr_teleoperate/free_arm_demo.py canonical coexistence pattern; 1+ year standing-mode evidence; eliminates D-06 firmware-default-zero risk without requiring bench-test gate. ROADMAP success criteria 1-2 updated accordingly in Plan 04-03 Task 2.]`
3. Do NOT change the D-06 heading, ID, category, or any other decision entries.
</action>

<acceptance_criteria>
- `grep -c 'RESOLVED 2026-05-14' .planning/phases/04-right-arm-only-executor/04-CONTEXT.md` returns `1`.
- `grep -c 'Option A chosen' .planning/phases/04-right-arm-only-executor/04-CONTEXT.md` returns `1`.
- The D-06 heading and ID string are unchanged — `grep -c 'D-06' 04-CONTEXT.md` returns the same count as before.
- No other decision entries are modified.
</acceptance_criteria>

## Task 4: Build verification

<read_first>
- src/unitree_g1_dex3_stack-main/CMakeLists.txt (sanity scan only — confirm executor target unchanged)
</read_first>

<action>
Run `colcon build --packages-select unitree_g1_dex3_stack` from the workspace root. Confirm exit code 0. The build output should show `joint_trajectory_executor.cpp` was recompiled (comment changes trigger a rebuild).
</action>

<acceptance_criteria>
- `colcon build --packages-select unitree_g1_dex3_stack` exits 0.
- Build log shows `joint_trajectory_executor.cpp` compiled.
- No new errors or warnings.
</acceptance_criteria>

</tasks>

<verification>
## Verification Steps

1. `colcon build --packages-select unitree_g1_dex3_stack` exits 0.
2. `grep -c 'Plan 04-03 D-06 Option A' src/unitree_g1_dex3_stack-main/src/joint_trajectory_executor.cpp` returns `3`.
3. `grep -c 'for (const auto& pair : joint_name_to_index)' src/unitree_g1_dex3_stack-main/src/joint_trajectory_executor.cpp` returns `4` (unchanged from post-04-02).
4. `grep -c 'LowCmd populates all 28 body motor command slots' .planning/ROADMAP.md` returns `1`.
5. `grep -c 'RESOLVED 2026-05-14' .planning/phases/04-right-arm-only-executor/04-CONTEXT.md` returns `1`.
6. `wc -l src/unitree_g1_dex3_stack-main/src/joint_trajectory_executor.cpp` shows exactly `+3` lines vs post-04-02.
</verification>

<threat_model>
## Security / Safety Threats Considered

- **D-06 firmware-default-zero risk**: ELIMINATED by Option A. Locking all 28 joints with kp=60 is the empirically verified coexistence pattern (Plan 01-10, 1+ year standing-mode operation, canonical in `free_arm_demo.py`). No firmware uncertainty remains for this path.
- **Non-right-arm joints fighting the running controller**: inherent in Option A; the running mode controller and the arm_sdk motion-sdk share authority via the master switch at 0.5. This is the same trade-off that Plan 01-10 shipped and that has been running; Phase 4 adds no new risk here.
- **Timing regression**: no code changes to loop bodies, timing constants, or snapshot logic. Zero regression risk for Plan 01-04..01-12 safety invariants.
- **ROADMAP criteria change is documentation-only**: updating criteria 1-2 in ROADMAP.md does not affect runtime behavior. The change corrects the documentation to match reality.

No HIGH severity threats. D-06 is resolved.
</threat_model>

<success_criteria>
- ROADMAP Phase 4 success criterion 1 (updated) satisfied: 28-joint lock active, right-arm q from trajectory.
- ROADMAP Phase 4 success criterion 2 (updated) satisfied: non-right-arm joints locked at latest_joint_positions_ with kp=60.
- ROADMAP Phase 4 success criterion 3 satisfied (delivered by Plan 04-01 — hand publishes removed).
- ROADMAP Phase 4 success criteria 4-5 remain as stated (running-mode coexistence properties to be observed in normal operation; no formal bench-test gate required per Option A choice).
- INTG-02 closed: right-arm trajectories execute via `LowCmd` with right-arm joints driven by trajectory values; non-right-arm joints locked at measured positions, preserving coexistence with running mode via the existing master-switch authority-sharing pattern.
- Plan 01-09/01-11/01-12 timing and snapshot patterns preserved — no compound-bug regression.
- D-06 decision recorded in CONTEXT.md and ROADMAP.md as resolved.
</success_criteria>
