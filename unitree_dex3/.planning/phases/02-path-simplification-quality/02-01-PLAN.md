---
phase: 02-path-simplification-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/unitree_g1_dex3_stack-main/src/ik_fcl_ompl_planner.cpp
autonomous: false
requirements:
  - PLAN-03

must_haves:
  truths:
    - "Planner logs 'Simplified: N → M waypoints (-X%)' for every successful solve, reflecting true simplification before interpolation"
    - "simplify_method parameter switches between simple (simplifySolution) and manual (PathSimplifier) at runtime"
    - "State validity resolution 0.01 enables inter-waypoint collision checking during planning"
    - "Simplified paths have same or fewer waypoints than raw solve output"
    - "All simplified paths remain collision-free (state validity checker reused by simplification)"
    - "Planner still publishes valid JointTrajectory messages after simplification"
  artifacts:
    - path: "src/unitree_g1_dex3_stack-main/src/ik_fcl_ompl_planner.cpp"
      provides: "Path simplification logic, new parameters, resolution enable, waypoint logging"
      contains: "simplifySolution|PathSimplifier|Simplified:"
  key_links:
    - from: "ss->solve()"
      to: "simplification branch"
      via: "simplify_method_ dispatch after getSolutionPath()"
      pattern: "simplify_method_.*simple|manual"
    - from: "simplification branch"
      to: "path.interpolate()"
      via: "sequential — simplify then interpolate (D-03 order)"
      pattern: "interpolate.*after.*simplif"
    - from: "setStateValidityCheckingResolution(0.01)"
      to: "ss->solve() and simplification"
      via: "called on SpaceInformation before solve(), automatically used by both solve and simplify"
---

<objective>
Add OMPL path simplification and state validity resolution checking to the right-arm planner.

Purpose: Remove unnecessary joint-space detours (e.g., arm going backward before reaching target) that RRTConnect produces, and enable inter-waypoint collision checking to ensure simplified paths are safe.

Output: Modified `ik_fcl_ompl_planner.cpp` with runtime-selectable simplification, resolution checking, and waypoint reduction logging.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-path-simplification-quality/02-CONTEXT.md
@src/unitree_g1_dex3_stack-main/src/ik_fcl_ompl_planner.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare simplification parameters and enable state validity resolution</name>
  <files>src/unitree_g1_dex3_stack-main/src/ik_fcl_ompl_planner.cpp</files>
  <read_first>
    - src/unitree_g1_dex3_stack-main/src/ik_fcl_ompl_planner.cpp (lines 15-19 for OMPL includes, lines 56-62 for existing declare_parameter calls, lines 93-99 for existing get_parameter calls, lines 215-222 for member variable declarations, lines 178-186 for parameter logging, line 578 for commented-out resolution line)
    - .planning/phases/02-path-simplification-quality/02-CONTEXT.md (D-01, D-02, D-04)
  </read_first>
  <action>
    **Add include** (per D-01 manual mode): Add `#include <ompl/geometric/PathSimplifier.h>` after the existing `#include <ompl/geometric/planners/rrt/RRTConnect.h>` at line 19.

    **Declare parameters** in `init()` (per D-01, D-02): After the existing `declare_parameter("collision_skip_pairs", ...)` at line 62, add:
    - `this->declare_parameter("simplify_method", std::string("simple"));` — per D-01, default "simple"
    - `this->declare_parameter("simplify_timeout", 0.5);` — per D-02, independent of planning_timeout
    - `this->declare_parameter("simplify_max_steps", 100);` — per D-01 manual mode, default 100
    - `this->declare_parameter("simplify_max_empty_steps", 50);` — per D-01 manual mode, default 50

    **Fetch parameters**: After the existing `get_parameter("collision_skip_pairs", ...)` at line 99, add corresponding `get_parameter` calls for all four new parameters into their member variables.

    **Add member variables**: In the Parameters section (after line 222, near `log_level_`), add:
    - `std::string simplify_method_ = "simple";`
    - `double simplify_timeout_ = 0.5;`
    - `int simplify_max_steps_ = 100;`
    - `int simplify_max_empty_steps_ = 50;`

    **Log new parameters**: After the existing `RCLCPP_INFO` for planner_type at line 186, add one INFO log line that prints all four new parameters: simplify_method_, simplify_timeout_, simplify_max_steps_, simplify_max_empty_steps_.

    **Enable state validity resolution** (per D-04): Uncomment line 578 — change `//ss->getSpaceInformation()->setStateValidityCheckingResolution(0.01);` to `ss->getSpaceInformation()->setStateValidityCheckingResolution(0.01);`. Use the literal 0.01, no new parameter.
  </action>
  <verify>
    <automated>cd /home/unitree/Desktop/unitree_dex3 && colcon build --packages-select unitree_g1_dex3_stack 2>&1 | tail -5 && echo "---" && grep -c 'declare_parameter.*simplify' src/unitree_g1_dex3_stack-main/src/ik_fcl_ompl_planner.cpp && grep -c 'get_parameter.*simplify' src/unitree_g1_dex3_stack-main/src/ik_fcl_ompl_planner.cpp && grep -c 'setStateValidityCheckingResolution' src/unitree_g1_dex3_stack-main/src/ik_fcl_ompl_planner.cpp && grep -c 'PathSimplifier.h' src/unitree_g1_dex3_stack-main/src/ik_fcl_ompl_planner.cpp</automated>
  </verify>
  <done>
    - Build succeeds (exit 0).
    - 4 new `declare_parameter` calls for simplify_method, simplify_timeout, simplify_max_steps, simplify_max_empty_steps.
    - 4 new `get_parameter` calls matching the declarations.
    - 4 new member variables with correct defaults.
    - `setStateValidityCheckingResolution(0.01)` uncommented (grep count = 1, no leading `//`).
    - `#include <ompl/geometric/PathSimplifier.h>` present.
    - INFO log line shows the four new parameter values at startup.
  </done>
</task>

<task type="auto">
  <name>Task 2: Insert simplification logic between solve() and interpolate() with waypoint logging</name>
  <files>src/unitree_g1_dex3_stack-main/src/ik_fcl_ompl_planner.cpp</files>
  <read_first>
    - src/unitree_g1_dex3_stack-main/src/ik_fcl_ompl_planner.cpp (lines 586-614 for the solve/interpolate/publish block after Task 1 edits)
    - .planning/phases/02-path-simplification-quality/02-CONTEXT.md (D-01, D-02, D-03, D-05, D-06)
  </read_first>
  <action>
    Inside the `if (ss->solve(planning_timeout_))` block, restructure the code between `solve()` and the trajectory conversion loop. The new flow per D-03:

    ```
    solve() → get solution path → snapshot n_before → simplify → re-get path → snapshot n_after → log reduction → interpolate() → convert → publish
    ```

    Concrete implementation:

    1. After `auto path = ss->getSolutionPath();` (currently line 587), capture pre-simplification count:
       `const std::size_t n_before = path.getStateCount();`

    2. Insert simplify_method_ dispatch (per D-01):
       - If `simplify_method_ == "simple"`: call `ss->simplifySolution(simplify_timeout_);` then reassign `path = ss->getSolutionPath();` to get the simplified result. simplifySolution() automatically reuses the existing state validity checker lambda — no extra wiring needed.
       - Else if `simplify_method_ == "manual"`: construct `ompl::geometric::PathSimplifier ps(ss->getSpaceInformation());` then call `ps.shortcutPath(path, static_cast<unsigned int>(simplify_max_steps_), static_cast<unsigned int>(simplify_max_empty_steps_));` followed by `ps.reduceVertices(path);`. For the manual path, D-02 requires respecting simplify_timeout_: if an outer timing check is needed, wrap the shortcutPath/reduceVertices in a wall-time guard (start a chrono timer before, WARN if elapsed > simplify_timeout_ after). The PathSimplifier operates on the path reference directly (in-place modification), so no reassignment needed.
       - Else (unknown value): log `RCLCPP_WARN` with the unrecognized simplify_method_ value and skip simplification (path used as-is). This preserves robustness per D-06.

    3. After simplification, capture post-simplification count:
       `const std::size_t n_after = path.getStateCount();`

    4. Log per D-05:
       `RCLCPP_INFO(this->get_logger(), "Simplified: %zu → %zu waypoints (-%zu%%)", n_before, n_after, n_before > 0 ? (n_before - n_after) * 100 / n_before : 0);`
       Use the right-arrow UTF-8 character `→` (U+2192) in the format string to match D-05 exactly. When N==M the output is `(-0%)` — no special case per D-05.

    5. Keep `path.interpolate();` AFTER the log line (per D-03 — log reflects true simplification, not interpolation densification).

    6. The rest of the code (trajectory conversion loop, publish) remains unchanged. Note: for the "simple" method, `path` was reassigned from `getSolutionPath()` after `simplifySolution()`; for "manual", `path` was modified in-place by PathSimplifier. Both produce the same downstream reference for `getStates()`.
  </action>
  <verify>
    <automated>cd /home/unitree/Desktop/unitree_dex3 && colcon build --packages-select unitree_g1_dex3_stack 2>&1 | tail -3 && echo "---" && grep -n 'simplifySolution\|PathSimplifier\|shortcutPath\|reduceVertices\|Simplified:' src/unitree_g1_dex3_stack-main/src/ik_fcl_ompl_planner.cpp && echo "---" && grep -n 'interpolate' src/unitree_g1_dex3_stack-main/src/ik_fcl_ompl_planner.cpp</automated>
  </verify>
  <done>
    - Build succeeds (exit 0).
    - `simplifySolution(simplify_timeout_)` called in the "simple" branch.
    - `og::PathSimplifier` constructed and `shortcutPath` called in the "manual" branch.
    - RCLCPP_INFO line with format "Simplified: %zu → %zu waypoints (-%zu%%)" present.
    - `path.interpolate()` appears AFTER the simplification block and log line (verified by line numbers).
    - Unknown simplify_method_ values produce a WARN log and skip simplification gracefully.
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Verify path simplification on G1 robot</name>
  <what-built>
    Path simplification with runtime-selectable method, state validity resolution checking at 0.01, and waypoint reduction logging. All changes in ik_fcl_ompl_planner.cpp.
  </what-built>
  <how-to-verify>
    1. Build and launch the planner:
       ```
       cd ~/Desktop/unitree_dex3
       colcon build --packages-select unitree_g1_dex3_stack
       source install/setup.bash
       ros2 launch unitree_g1_dex3_stack planner.launch.py
       ```
    2. Observe startup logs — confirm the four new parameters are printed (simplify_method=simple, simplify_timeout=0.5, simplify_max_steps=100, simplify_max_empty_steps=50).
    3. Send a /goal_pose and observe the terminal output. Expected:
       - `"Simplified: N → M waypoints (-X%)"` line appears after each successful solve.
       - M ≤ N (waypoints reduced or unchanged for short paths).
       - Arm reaches the goal without visible backward detour (the primary user complaint from Phase 1).
       - No collision warnings during planning.
    4. Send 3-5 different goal poses. Path counts may vary per D-06 (RRTConnect randomness), but simplification log should always appear.
    5. (Optional) Test manual mode: stop, set `simplify_method:=manual` in launch params, re-launch, send goals. Verify log still appears and paths are reasonable.
  </how-to-verify>
  <resume-signal>Type "approved" if simplification works correctly, or describe any issues observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /goal_pose topic → planner callback | Untrusted pose input triggers planning; pre-existing boundary from Phase 1 |
| ROS 2 parameters → planner config | Operator-controlled parameters set at launch time on local robot |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | simplify_timeout param | accept | Operator has local control over ROS params; setting extreme values (e.g., 999s) only delays their own planning. No remote attack surface. |
| T-02-02 | Denial of Service | simplify_method param | accept | Invalid values produce a WARN log and skip simplification; planner still publishes unsimplified path. No crash path. |
| T-02-03 | Information Disclosure | RCLCPP_INFO log | accept | Waypoint counts are non-sensitive operational data on a local robot network. |
</threat_model>

<verification>
## Phase-Level Verification

1. **Build**: `colcon build --packages-select unitree_g1_dex3_stack` exits 0.
2. **Parameter presence**: `grep -c 'declare_parameter.*simplify' src/unitree_g1_dex3_stack-main/src/ik_fcl_ompl_planner.cpp` returns 4.
3. **Resolution enabled**: `grep 'setStateValidityCheckingResolution' src/unitree_g1_dex3_stack-main/src/ik_fcl_ompl_planner.cpp | grep -v '//'` returns 1 match (uncommented).
4. **Simplification present**: `grep -c 'simplifySolution\|shortcutPath' src/unitree_g1_dex3_stack-main/src/ik_fcl_ompl_planner.cpp` returns ≥ 2 (one per method).
5. **Log format**: `grep 'Simplified:' src/unitree_g1_dex3_stack-main/src/ik_fcl_ompl_planner.cpp` matches the D-05 format.
6. **Interpolate order**: `grep -n 'Simplified:\|interpolate' src/unitree_g1_dex3_stack-main/src/ik_fcl_ompl_planner.cpp` — the Simplified log line number is LESS than the interpolate line number (D-03 order).
7. **No deferred ideas**: `grep -c 'planner_seed\|simplify_method.*none' src/unitree_g1_dex3_stack-main/src/ik_fcl_ompl_planner.cpp` returns 0.
</verification>

<success_criteria>
1. OMPL path simplification called after solve(), before trajectory conversion — ROADMAP success criterion 1.
2. `setStateValidityCheckingResolution(0.01)` enabled — ROADMAP success criterion 2.
3. Waypoint reduction logged as "Simplified: N → M waypoints (-X%)" — ROADMAP success criterion 3.
4. Simplified paths remain collision-free (simplification reuses the state validity checker) — ROADMAP success criterion 4.
5. All six CONTEXT.md decisions (D-01 through D-06) implemented as specified.
6. PLAN-03 requirement fully delivered.
</success_criteria>

<output>
After completion, create `.planning/phases/02-path-simplification-quality/02-01-SUMMARY.md`
</output>
