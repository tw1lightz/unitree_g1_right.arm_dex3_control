---
phase: 5
plan: "05-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/unitree_g1_dex3_stack-main/launch/reach.launch.py
autonomous: true
requirements_addressed: [INTG-03]
---

<objective>
创建 `launch/reach.launch.py`，通过 IncludeLaunchDescription 组合 4 个子 launch（robot、perception、planner、control）加上 keyboard_trigger_node 和 d435→camera_link 静态 TF，实现单条命令启动完整流水线。
</objective>

<tasks>

## Task 1: 创建 reach.launch.py

<read_first>
- src/unitree_g1_dex3_stack-main/launch/visual_detect_yolo.launch.py
- src/unitree_g1_dex3_stack-main/launch/robot.launch.py
- src/unitree_g1_dex3_stack-main/launch/perception.launch.py
- src/unitree_g1_dex3_stack-main/launch/planner.launch.py
- src/unitree_g1_dex3_stack-main/launch/control.launch.py
- .planning/phases/05-end-to-end-integration/05-CONTEXT.md
- .planning/phases/05-end-to-end-integration/05-RESEARCH.md
</read_first>

<action>
创建 `src/unitree_g1_dex3_stack-main/launch/reach.launch.py`：

1. 声明 4 个顶层参数（D-02）：
   - `model_path`，默认 `/home/unitree/Desktop/unitree_dex3/best.pt`
   - `target_class`，默认 `bottle`
   - `imshow`，默认 `true`
   - `planning_timeout`，默认 `1.0`

2. 立即启动 `robot.launch.py`（IncludeLaunchDescription），无额外参数（使用默认 collision_primitives URDF）。CycloneDDS 环境变量由 robot.launch.py 内部的 SetEnvironmentVariable 自动设置（D-04）。

3. 添加 `d435_link` → `camera_link` 静态 TF 发布节点（从 visual_detect_yolo.launch.py 复制）：
   - package: `tf2_ros`
   - executable: `static_transform_publisher`
   - arguments: `['0', '0', '0', '0', '0', '0', 'd435_link', 'camera_link']`

4. 用 `TimerAction(period=3.0)` 包裹以下 3 个 include + 1 个节点（D-03）：
   - `perception.launch.py`，传参：model_path、imshow、allowed_classes（格式 `["['", LaunchConfiguration('target_class'), "']"]`）
   - `planner.launch.py`，传参：planning_timeout
   - `control.launch.py`，无额外参数
   - `keyboard_trigger_node.py` Node（package=unitree_g1_dex3_stack, executable=keyboard_trigger_node.py, emulate_tty=True）

5. generate_launch_description() 返回 LaunchDescription 包含：参数声明 + robot_launch + d435_tf_node + TimerAction
</action>

<acceptance_criteria>
- 文件存在于 `src/unitree_g1_dex3_stack-main/launch/reach.launch.py`
- 文件包含 `DeclareLaunchArgument('model_path'`
- 文件包含 `DeclareLaunchArgument('target_class'`
- 文件包含 `DeclareLaunchArgument('imshow'`
- 文件包含 `DeclareLaunchArgument('planning_timeout'`
- 文件包含 `IncludeLaunchDescription` 引用 `robot.launch.py`
- 文件包含 `IncludeLaunchDescription` 引用 `perception.launch.py`
- 文件包含 `IncludeLaunchDescription` 引用 `planner.launch.py`
- 文件包含 `IncludeLaunchDescription` 引用 `control.launch.py`
- 文件包含 `static_transform_publisher` 节点，arguments 含 `d435_link` 和 `camera_link`
- 文件包含 `TimerAction(period=3.0`
- 文件包含 `keyboard_trigger_node.py` 节点声明
- 文件包含 `emulate_tty=True`
- `python3 -c "import ast; ast.parse(open('src/unitree_g1_dex3_stack-main/launch/reach.launch.py').read())"` 退出码 0
</acceptance_criteria>

</tasks>

<verification>
1. `python3 -c "import ast; ast.parse(open('src/unitree_g1_dex3_stack-main/launch/reach.launch.py').read())"` — 语法正确
2. `grep -c 'IncludeLaunchDescription' src/unitree_g1_dex3_stack-main/launch/reach.launch.py` — 输出 4
3. `grep 'd435_link' src/unitree_g1_dex3_stack-main/launch/reach.launch.py` — 有匹配
4. `grep 'TimerAction' src/unitree_g1_dex3_stack-main/launch/reach.launch.py` — 有匹配
</verification>

<must_haves>
- reach.launch.py 通过 IncludeLaunchDescription 组合 4 个子 launch
- d435_link→camera_link 静态 TF 在 reach.launch.py 中发布（不在子 launch 中）
- TimerAction(3.0) 延迟 perception/planner/control/keyboard 启动
- 顶层仅暴露 4 个参数：model_path、target_class、imshow、planning_timeout
- target_class 通过 `["['", LaunchConfiguration('target_class'), "']"]` 格式传给 perception 的 allowed_classes
</must_haves>
