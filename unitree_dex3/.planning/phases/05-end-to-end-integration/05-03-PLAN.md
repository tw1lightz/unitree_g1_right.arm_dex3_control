---
phase: 5
plan: "05-03"
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - src/unitree_g1_dex3_stack-main/README.md
  - src/unitree_g1_dex3_stack-main/docs/ARCHITECTURE.md
autonomous: true
requirements_addressed: [INTG-01, INTG-03]
---

<objective>
重写 README.md 为最小实用版（环境准备 + 一条启动命令 + 按 K 触发 + 参数覆盖示例），新建 docs/ARCHITECTURE.md 完整文档（系统架构、话题列表、节点说明、参数表、决策背景、故障排查）。
</objective>

<tasks>

## Task 1: 重写 README.md

<read_first>
- src/unitree_g1_dex3_stack-main/README.md
- src/unitree_g1_dex3_stack-main/launch/reach.launch.py
- .planning/phases/05-end-to-end-integration/05-CONTEXT.md
</read_first>

<action>
原地重写 `src/unitree_g1_dex3_stack-main/README.md` 为最小实用版（D-12）：

结构：
1. 标题：`# unitree_g1_dex3_stack`
2. 一句话描述：Unitree G1 右臂安全抓取 ROS 2 全栈
3. `## 环境准备`：
   - ROS 2 Humble
   - `pip install ultralytics torch`（D-11）
   - `sudo apt install ros-humble-realsense2-camera ros-humble-vision-msgs`
   - colcon build 命令（含 `-DBUILD_IK_FCL_OMPL_PLANNER=ON`）
4. `## 快速启动`：
   - `ros2 launch unitree_g1_dex3_stack reach.launch.py`
   - 说明：启动后按 K 键触发抓取
5. `## 参数覆盖`：
   - `target_class:=cup`
   - `imshow:=false`
   - `planning_timeout:=2.0`
   - `model_path:=/path/to/model.pt`
6. `## 架构`：指向 `docs/ARCHITECTURE.md`
</action>

<acceptance_criteria>
- README.md 包含 `pip install ultralytics torch`
- README.md 包含 `ros2 launch unitree_g1_dex3_stack reach.launch.py`
- README.md 包含 `按 K` 或 `press K` 触发说明
- README.md 包含 `target_class` 参数覆盖示例
- README.md 包含指向 `docs/ARCHITECTURE.md` 的链接
- README.md 行数 < 80（最小实用版）
</acceptance_criteria>

## Task 2: 创建 docs/ARCHITECTURE.md

<read_first>
- .planning/phases/05-end-to-end-integration/05-CONTEXT.md
- .planning/phases/05-end-to-end-integration/05-RESEARCH.md
- .planning/phases/04-right-arm-only-executor/04-CONTEXT.md
- .planning/phases/01-right-arm-only-planner/01-CONTEXT.md
- src/unitree_g1_dex3_stack-main/launch/reach.launch.py
</read_first>

<action>
创建 `src/unitree_g1_dex3_stack-main/docs/ARCHITECTURE.md`（D-13）：

结构：
1. `# 系统架构`
2. `## 概览` — 一段话描述系统：YOLO 检测 → 3D 投影 → 键盘触发 → OMPL 规划 → 右臂执行
3. `## 节点与话题流` — ASCII 数据流图：
   - ultralytics_detector → /yolo/bounding_boxes
   - project_to_3d_node → /detections_3d
   - keyboard_trigger_node → /goal_pose
   - ik_fcl_ompl_planner → /planned_trajectory
   - joint_trajectory_executor → LowCmd
4. `## 节点说明` — 每个节点一小段：功能、订阅、发布
5. `## 话题列表` — 表格：话题名、消息类型、发布者、订阅者
6. `## 参数表` — 表格：reach.launch.py 的 4 个顶层参数 + 各子 launch 关键参数
7. `## 关键决策背景`：
   - Phase 1：为什么用 OMPL+FCL+TRAC-IK 而非 MoveIt 2
   - Phase 2：路径简化（PathSimplifier）
   - Phase 3：velocity_scale=0.2 基于 URDF 速度限制
   - Phase 4：28 关节锁定 kp=60（D-06 Option A，匹配 free_arm_demo.py）
   - Phase 5：单条启动命令 + 键盘触发设计
8. `## 故障排查` — 常见问题：TF 超时、CycloneDDS 网络、YOLO 模型路径、关节限位报错
</action>

<acceptance_criteria>
- 文件存在于 `src/unitree_g1_dex3_stack-main/docs/ARCHITECTURE.md`
- 文件包含 `## 节点与话题流`
- 文件包含 `## 话题列表`
- 文件包含 `## 参数表`
- 文件包含 `## 关键决策背景`
- 文件包含 `OMPL` 和 `MoveIt` 的对比说明
- 文件包含 `28 关节` 或 `28-joint` 锁定说明
- 文件包含 `## 故障排查`
- 文件包含 `/goal_pose` 和 `/detections_3d` 话题
</acceptance_criteria>

</tasks>

<verification>
1. `test -f src/unitree_g1_dex3_stack-main/docs/ARCHITECTURE.md` — 退出码 0
2. `wc -l src/unitree_g1_dex3_stack-main/README.md` — 行数 < 80
3. `grep -c '##' src/unitree_g1_dex3_stack-main/docs/ARCHITECTURE.md` — 至少 7 个二级标题
</verification>

<must_haves>
- README 是最小实用版：环境准备、一条启动命令、K 键触发、参数覆盖
- ARCHITECTURE.md 包含完整数据流图、节点说明、话题表、参数表
- ARCHITECTURE.md 包含各阶段关键决策背景（Phase 1-5）
- ARCHITECTURE.md 包含故障排查章节
</must_haves>
