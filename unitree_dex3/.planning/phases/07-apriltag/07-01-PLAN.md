---
phase: 7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/unitree_g1_dex3_stack-main/CMakeLists.txt
  - src/unitree_g1_dex3_stack-main/package.xml
  - src/unitree_g1_dex3_stack-main/config/apriltag.yaml
autonomous: true
requirements:
  - TAG-02
---

# Plan 07-01: 构建配置 + YAML 参数文件

<objective>
为 Phase 7 准备构建脚手架：在 CMakeLists.txt 中安装 Python 节点和 config 目录、在 package.xml 中声明运行期依赖、创建 `config/apriltag.yaml` 参数文件（覆盖 D-08 的全部 11 个字段）。本计划完成后 colcon build 须仍然成功，即便 Python 节点本身在下一个计划才创建（install(PROGRAMS ...) 在 build 时不要求文件存在，仅在 build 完成时验证；通过 install 阶段会捕获缺失文件 — 本计划在 Plan 02 创建脚本之后才需 build 验证）。
</objective>

<must_haves>
## Goal-Backward Verification
- truth: "src/unitree_g1_dex3_stack-main/config/apriltag.yaml exists"
- truth: "config/apriltag.yaml top-level key is 'apriltag_detector' with nested 'ros__parameters' block (matches Node name in Plan 02)"
- truth: "config/apriltag.yaml declares all 11 fields from CONTEXT D-08: tag_family, tag_size, target_tag_id, offset_xyz, decision_margin_min, output_frame, rgb_topic, camera_info_topic, tag_pose_topic, target_pose_topic, tf_lookup_timeout_s"
- truth: "config/apriltag.yaml does NOT contain an imshow field (per D-15 imshow is a launch arg, not a YAML param)"
- truth: "CMakeLists.txt install(PROGRAMS ...) block contains scripts/apriltag_detector_node.py"
- truth: "CMakeLists.txt has a separate install(DIRECTORY config DESTINATION share/${PROJECT_NAME}) block (not merged with launch/robots block)"
- truth: "package.xml contains <exec_depend>realsense2_camera</exec_depend>"
- truth: "package.xml contains <exec_depend>python3-opencv</exec_depend>"
- truth: "package.xml does NOT contain pupil-apriltags or pupil_apriltags (pip dependency, not in rosdep)"
- truth: "Existing CMakeLists install(PROGRAMS) entries (tcp_torso_pose.py, keyboard_trigger_node.py) are still present (additive change)"
- truth: "Existing package.xml dependencies (cv_bridge, tf2_ros, tf2_geometry_msgs, OpenCV, vision_msgs) are still present (additive change)"
</must_haves>

<truths>
## Cross-cutting Constraints
- truth: "Phase 7 install changes are purely additive — no removal of existing build targets, find_package, install entries, or dependencies"
- truth: "config/apriltag.yaml node-name key MUST be 'apriltag_detector' to match the Node('apriltag_detector') instantiation in Plan 02"
- truth: "pupil-apriltags is intentionally NOT in package.xml (pip-only); install hint goes in README in Plan 03"
</truths>

<tasks>
## Tasks

### Task 1: Create config/apriltag.yaml
<read_first>
- src/unitree_g1_dex3_stack-main/CMakeLists.txt (confirm install pattern + DESTINATION conventions)
- .planning/phases/07-apriltag/07-CONTEXT.md (D-08 full field list — 11 fields)
- .planning/phases/07-apriltag/07-PATTERNS.md (apriltag.yaml section — concrete schema)
</read_first>

<action>
Create the file `src/unitree_g1_dex3_stack-main/config/apriltag.yaml` with this exact ROS 2 parameter format:

- Top-level key `apriltag_detector` (must match the Node name used in Plan 02's `apriltag_detector_node.py` — `Node('apriltag_detector')`).
- Single nested `ros__parameters` block.
- All 11 fields from CONTEXT D-08, with the specified default values:
  - tag_family: "tag36h11"
  - tag_size: 0.08
  - target_tag_id: 0
  - offset_xyz: [0.0, 0.0, 0.05]
  - decision_margin_min: 25.0
  - output_frame: "torso_link"
  - rgb_topic: "/camera/color/image_raw"
  - camera_info_topic: "/camera/color/camera_info"
  - tag_pose_topic: "/apriltag/tag_pose"
  - target_pose_topic: "/apriltag/target_pose"
  - tf_lookup_timeout_s: 0.5
- Inline comment headers grouping the fields (detection / offset / filtering / topics & frames / TF) for readability per CONTEXT discretion ("YAML 注释只给关键字段写说明").
- Do NOT include an `imshow` field (D-15 — imshow is a launch arg, not a YAML param).
- File MUST end with a trailing newline.
</action>

<acceptance_criteria>
- `test -f src/unitree_g1_dex3_stack-main/config/apriltag.yaml` exits 0
- `head -1 src/unitree_g1_dex3_stack-main/config/apriltag.yaml` shows a `#` comment (banner line)
- `grep -E "^apriltag_detector:" src/unitree_g1_dex3_stack-main/config/apriltag.yaml` returns a match
- `grep -E "^\s*ros__parameters:" src/unitree_g1_dex3_stack-main/config/apriltag.yaml` returns a match
- `grep -cE "^\s*(tag_family|tag_size|target_tag_id|offset_xyz|decision_margin_min|output_frame|rgb_topic|camera_info_topic|tag_pose_topic|target_pose_topic|tf_lookup_timeout_s):" src/unitree_g1_dex3_stack-main/config/apriltag.yaml` returns 11
- `grep -E "tag_family:\s*\"tag36h11\"" src/unitree_g1_dex3_stack-main/config/apriltag.yaml` returns a match
- `grep -E "tag_size:\s*0\.08" src/unitree_g1_dex3_stack-main/config/apriltag.yaml` returns a match
- `grep -E "target_tag_id:\s*0" src/unitree_g1_dex3_stack-main/config/apriltag.yaml` returns a match
- `grep -E "offset_xyz:\s*\[0\.0,\s*0\.0,\s*0\.05\]" src/unitree_g1_dex3_stack-main/config/apriltag.yaml` returns a match
- `grep -E "decision_margin_min:\s*25\.0" src/unitree_g1_dex3_stack-main/config/apriltag.yaml` returns a match
- `grep -E "output_frame:\s*\"torso_link\"" src/unitree_g1_dex3_stack-main/config/apriltag.yaml` returns a match
- `grep -E "tag_pose_topic:\s*\"/apriltag/tag_pose\"" src/unitree_g1_dex3_stack-main/config/apriltag.yaml` returns a match
- `grep -E "target_pose_topic:\s*\"/apriltag/target_pose\"" src/unitree_g1_dex3_stack-main/config/apriltag.yaml` returns a match
- `grep -E "tf_lookup_timeout_s:\s*0\.5" src/unitree_g1_dex3_stack-main/config/apriltag.yaml` returns a match
- `grep -c "^\s*imshow:" src/unitree_g1_dex3_stack-main/config/apriltag.yaml` returns 0 (imshow is launch arg, not YAML)
- `python3 -c "import yaml; yaml.safe_load(open('src/unitree_g1_dex3_stack-main/config/apriltag.yaml'))"` exits 0 (file is valid YAML)

### Task 2: Update CMakeLists.txt — add Python script + config install
<read_first>
- src/unitree_g1_dex3_stack-main/CMakeLists.txt
- .planning/phases/07-apriltag/07-PATTERNS.md (CMakeLists section — current install(PROGRAMS) block at L123-128 and install(DIRECTORY) block at L130-133)
</read_first>

<action>
Edit `src/unitree_g1_dex3_stack-main/CMakeLists.txt`:

1. In the existing `install(PROGRAMS ...)` block (lines ~123-128 — currently lists `scripts/tcp_torso_pose.py` and `scripts/keyboard_trigger_node.py`), add a new line `scripts/apriltag_detector_node.py` BEFORE the closing `DESTINATION lib/${PROJECT_NAME}` line. Place it after `scripts/keyboard_trigger_node.py` (end-of-list).

2. After the existing `install(DIRECTORY launch robots ...)` block (lines ~130-133), insert a NEW separate install(DIRECTORY ...) block for `config`:
   ```cmake
   install(DIRECTORY
     config
     DESTINATION share/${PROJECT_NAME}
   )
   ```
   Do NOT merge `config` into the existing `launch robots` block — keep concerns separate (matches Phase 6 incremental-change convention).

3. Do NOT modify any `find_package`, `add_executable`, `ament_target_dependencies`, `target_link_libraries`, `BUILD_IK_FCL_OMPL_PLANNER` block, or any other existing line. Phase 7 is Python-only.
</action>

<acceptance_criteria>
- `grep -c "scripts/apriltag_detector_node.py" src/unitree_g1_dex3_stack-main/CMakeLists.txt` returns 1
- `grep -c "scripts/tcp_torso_pose.py" src/unitree_g1_dex3_stack-main/CMakeLists.txt` returns 1 (existing entry preserved)
- `grep -c "scripts/keyboard_trigger_node.py" src/unitree_g1_dex3_stack-main/CMakeLists.txt` returns 1 (existing entry preserved)
- `grep -A2 "install(DIRECTORY" src/unitree_g1_dex3_stack-main/CMakeLists.txt | grep -c "config" ` returns at least 1 (config appears in an install(DIRECTORY) block)
- `grep -c "^install(DIRECTORY" src/unitree_g1_dex3_stack-main/CMakeLists.txt` returns 2 (original launch/robots block + new config block)
- `grep -A4 "^install(DIRECTORY$" src/unitree_g1_dex3_stack-main/CMakeLists.txt | grep -c "config"` returns 1 (config in its own block, not mixed with launch/robots)
- `grep -c "find_package(ament_cmake REQUIRED)" src/unitree_g1_dex3_stack-main/CMakeLists.txt` returns 1 (find_package block untouched)
- `grep -c "add_executable(visual_detection_tester" src/unitree_g1_dex3_stack-main/CMakeLists.txt` returns 1 (existing C++ targets preserved)
- `grep -c "BUILD_IK_FCL_OMPL_PLANNER" src/unitree_g1_dex3_stack-main/CMakeLists.txt` returns at least 4 (option declaration + 3 conditional blocks preserved)

### Task 3: Update package.xml — add runtime exec_depends
<read_first>
- src/unitree_g1_dex3_stack-main/package.xml
- .planning/phases/07-apriltag/07-PATTERNS.md (package.xml section — current depend block at L28-43)
</read_first>

<action>
Edit `src/unitree_g1_dex3_stack-main/package.xml`:

1. After the existing `<depend>tf2_geometry_msgs</depend>` line (last line of the perception/TF dep block, around L43), insert a comment header and two new `<exec_depend>` entries:

   ```xml
     <!-- AprilTag detector runtime deps (Phase 7) -->
     <exec_depend>realsense2_camera</exec_depend>
     <exec_depend>python3-opencv</exec_depend>
   ```

2. Use `<exec_depend>` (not `<depend>`) for both — these are runtime-only consumers from Python; ament should not try to build-link against them.

3. Do NOT add `pupil-apriltags` (pip-only, not in rosdep). Its install hint goes in README in Plan 03.

4. Do NOT remove or alter any existing `<depend>`, `<buildtool_depend>`, or `<export>` entries. Confirm that `cv_bridge`, `tf2_ros`, `tf2_geometry_msgs`, `OpenCV`, `vision_msgs` remain.

5. File must remain valid XML (single root `<package>` element, well-formed).
</action>

<acceptance_criteria>
- `grep -c "<exec_depend>realsense2_camera</exec_depend>" src/unitree_g1_dex3_stack-main/package.xml` returns 1
- `grep -c "<exec_depend>python3-opencv</exec_depend>" src/unitree_g1_dex3_stack-main/package.xml` returns 1
- `grep -c "pupil[-_]apriltags" src/unitree_g1_dex3_stack-main/package.xml` returns 0
- `grep -c "<depend>cv_bridge</depend>" src/unitree_g1_dex3_stack-main/package.xml` returns 1 (preserved)
- `grep -c "<depend>tf2_ros</depend>" src/unitree_g1_dex3_stack-main/package.xml` returns 1 (preserved)
- `grep -c "<depend>tf2_geometry_msgs</depend>" src/unitree_g1_dex3_stack-main/package.xml` returns 1 (preserved)
- `grep -c "<depend>OpenCV</depend>" src/unitree_g1_dex3_stack-main/package.xml` returns 1 (preserved)
- `grep -c "<depend>vision_msgs</depend>" src/unitree_g1_dex3_stack-main/package.xml` returns 1 (preserved)
- `python3 -c "import xml.etree.ElementTree as ET; ET.parse('src/unitree_g1_dex3_stack-main/package.xml')"` exits 0 (valid XML)
- `xmllint --noout src/unitree_g1_dex3_stack-main/package.xml` exits 0 (well-formed XML; if xmllint not installed, the python check above suffices)
</acceptance_criteria>
</tasks>

<verification>
## Verification

This plan does NOT run `colcon build` directly — `apriltag_detector_node.py` doesn't exist yet (Plan 02 creates it). `colcon build` would fail at the install step ("file not found: scripts/apriltag_detector_node.py").

The integrated colcon build verification is in Plan 02 Task 5 (after the script exists).

What this plan does verify:
- YAML schema is parseable and contains all 11 D-08 fields
- CMakeLists changes are present and additive
- package.xml is still valid XML and has the new exec_depends
</verification>

<success_criteria>
## Success Criteria
- config/apriltag.yaml exists, is valid YAML, contains all 11 fields from D-08
- CMakeLists.txt has `scripts/apriltag_detector_node.py` in install(PROGRAMS) and a new install(DIRECTORY config ...) block
- package.xml has the two new `<exec_depend>` lines and is still valid XML
- All existing build targets, dependencies, and install entries are preserved (additive change only)
- Plan 02 can now create the Python node and Plan 03 can launch it without build-config blockers
</success_criteria>
