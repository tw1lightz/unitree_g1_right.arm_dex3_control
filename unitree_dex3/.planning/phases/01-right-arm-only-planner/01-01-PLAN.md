---
phase: 01-right-arm-only-planner
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/unitree_g1_dex3_stack-main/src/ik_fcl_ompl_planner.cpp
  - src/unitree_g1_dex3_stack-main/launch/robot.launch.py
  - src/unitree_g1_dex3_stack-main/launch/planner.launch.py
autonomous: true
requirements: [PLAN-01]

must_haves:
  truths:
    - "After build, the planner source file contains no left-arm chain/IK/FK identifiers and no y-axis arm selector."
    - "After build, robot.launch.py defaults to the collision-primitives URDF."
    - "After build, planner.launch.py no longer declares the left_tip parameter."
    - "After build, isInCollision takes no `bool use_right` parameter."
    - "At runtime, the planner always uses the right arm chain regardless of the goal pose y-coordinate."
  artifacts:
    - path: "src/unitree_g1_dex3_stack-main/src/ik_fcl_ompl_planner.cpp"
      provides: "Right-arm-only planner source with all left-arm infrastructure removed"
      min_lines: 600
    - path: "src/unitree_g1_dex3_stack-main/launch/robot.launch.py"
      provides: "Robot launch defaulting to collision-primitives URDF"
      contains: "g1_29dof_lock_waist_with_hand_rev_1_0_collision_primitives.urdf"
    - path: "src/unitree_g1_dex3_stack-main/launch/planner.launch.py"
      provides: "Planner launch without left_tip argument"
  key_links:
    - from: "src/unitree_g1_dex3_stack-main/launch/robot.launch.py"
      to: "src/unitree_g1_dex3_stack-main/robots/g1_description/g1_29dof_lock_waist_with_hand_rev_1_0_collision_primitives.urdf"
      via: "default urdf_name argument"
      pattern: "g1_29dof_lock_waist_with_hand_rev_1_0_collision_primitives\\.urdf"
---

<objective>
Strip all left-arm infrastructure from `ik_fcl_ompl_planner.cpp`, remove the y-axis arm selector so the planner always uses the right arm, and switch the default URDF to the collision-primitives version.

Purpose: Establishes the right-arm-only foundation that Plan 02 will build on. After this plan, the planner has a single-arm code path (no left-arm members, methods, params, or logs), the y-axis goal-position selector is gone (D-02 satisfied), the launch system loads the fast collision URDF (D-04 satisfied), and the planner.launch.py no longer advertises a `left_tip` parameter.

Output: A planner that always plans for the right arm regardless of goal y-coordinate, with a still-broken collision filter and missing non-arm transforms (Plan 02's job).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-right-arm-only-planner/01-CONTEXT.md
@.planning/phases/01-right-arm-only-planner/01-RESEARCH.md
@src/unitree_g1_dex3_stack-main/src/ik_fcl_ompl_planner.cpp
@src/unitree_g1_dex3_stack-main/launch/robot.launch.py
@src/unitree_g1_dex3_stack-main/launch/planner.launch.py
@src/unitree_g1_dex3_stack-main/include/g1_dex3_joint_defs.hpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Switch robot.launch.py default URDF to collision-primitives variant</name>
  <files>src/unitree_g1_dex3_stack-main/launch/robot.launch.py</files>
  <read_first>
    src/unitree_g1_dex3_stack-main/launch/robot.launch.py,
    src/unitree_g1_dex3_stack-main/robots/g1_description/g1_29dof_lock_waist_with_hand_rev_1_0_collision_primitives.urdf,
    .planning/phases/01-right-arm-only-planner/01-CONTEXT.md
  </read_first>
  <action>
In `src/unitree_g1_dex3_stack-main/launch/robot.launch.py`, change the `default_value` of the `urdf_name` `DeclareLaunchArgument` from `'g1_29dof_lock_waist_with_hand_rev_1_0.urdf'` to `'g1_29dof_lock_waist_with_hand_rev_1_0_collision_primitives.urdf'`.

Do not modify the description string, the `urdf_path` argument, or any other launch logic. Do not add comments. The file's behavior must be: when launched without arguments, it loads the collision-primitives URDF; when launched with `urdf_name:=...` or `urdf_path:=...`, the override still wins.

Concrete edit (line 48 in current file):
- Before: `default_value='g1_29dof_lock_waist_with_hand_rev_1_0.urdf',`
- After:  `default_value='g1_29dof_lock_waist_with_hand_rev_1_0_collision_primitives.urdf',`
  </action>
  <verify>
grep -n "default_value=.*g1_29dof_lock_waist_with_hand_rev_1_0_collision_primitives.urdf" src/unitree_g1_dex3_stack-main/launch/robot.launch.py
  </verify>
  <acceptance_criteria>
    - `src/unitree_g1_dex3_stack-main/launch/robot.launch.py` contains the literal string `g1_29dof_lock_waist_with_hand_rev_1_0_collision_primitives.urdf`.
    - The non-primitives default `g1_29dof_lock_waist_with_hand_rev_1_0.urdf` no longer appears as a `default_value` in this file (use `grep -n "default_value=.*g1_29dof_lock_waist_with_hand_rev_1_0.urdf'" src/unitree_g1_dex3_stack-main/launch/robot.launch.py` — should return zero matches; the collision-primitives string contains the suffix and is OK).
    - `urdf_path` argument is preserved unchanged.
  </acceptance_criteria>
  <done>Default URDF resolves to the collision-primitives variant when robot.launch.py is invoked without `urdf_name:=` override.</done>
</task>

<task type="auto">
  <name>Task 2: Remove left_tip from planner.launch.py</name>
  <files>src/unitree_g1_dex3_stack-main/launch/planner.launch.py</files>
  <read_first>
    src/unitree_g1_dex3_stack-main/launch/planner.launch.py
  </read_first>
  <action>
In `src/unitree_g1_dex3_stack-main/launch/planner.launch.py`, remove all references to `left_tip`:

1. Delete the line `left_tip = str(LaunchConfiguration('left_tip').perform(context))` (currently line 16).
2. Delete the dict entry `'left_tip': left_tip,` from the `parameters` list passed to the planner Node (currently line 32).
3. Delete the `DeclareLaunchArgument('left_tip', default_value='left_wrist_yaw_link'),` line (currently line 52).

Leave `right_tip` untouched. Leave `base_link`, `trajectory_time_step`, `planning_timeout`, `detection_topic`, `selected_class_topic`, `planner_type`, `collision_skip_pairs` untouched. Do not add comments. After the edits, `grep -n 'left_tip' src/unitree_g1_dex3_stack-main/launch/planner.launch.py` must return zero matches.
  </action>
  <verify>
grep -nE 'left_tip|left_arm|left_wrist' src/unitree_g1_dex3_stack-main/launch/planner.launch.py
  </verify>
  <acceptance_criteria>
    - `grep -n 'left_tip' src/unitree_g1_dex3_stack-main/launch/planner.launch.py` returns zero matches.
    - `grep -n 'right_tip' src/unitree_g1_dex3_stack-main/launch/planner.launch.py` still returns at least 2 matches (declaration + parameters dict + perform).
    - The `parameters=[{...}]` dict remains syntactically valid Python (no trailing comma issues, no orphan keys).
    - `python3 -c "import ast; ast.parse(open('src/unitree_g1_dex3_stack-main/launch/planner.launch.py').read())"` exits 0.
  </acceptance_criteria>
  <done>Planner launch file no longer declares or forwards the left_tip parameter, and parses cleanly as Python.</done>
</task>

<task type="auto">
  <name>Task 3: Remove all left-arm code and the y-axis arm selector from ik_fcl_ompl_planner.cpp</name>
  <files>src/unitree_g1_dex3_stack-main/src/ik_fcl_ompl_planner.cpp</files>
  <read_first>
    src/unitree_g1_dex3_stack-main/src/ik_fcl_ompl_planner.cpp,
    .planning/phases/01-right-arm-only-planner/01-CONTEXT.md,
    .planning/phases/01-right-arm-only-planner/01-RESEARCH.md
  </read_first>
  <action>
Delete every left-arm artifact AND the y-axis arm selector from `src/unitree_g1_dex3_stack-main/src/ik_fcl_ompl_planner.cpp`. After this task, no token containing `left_tip`, `kdl_chain_left`, `ik_left`, `fk_left_solver`, `left_lower`, `left_upper`, `left arm`, `left_chain_oss`, or `use_right` may remain in the file. Specific edits:

1. **Parameter declaration / fetch** (constructor, around lines 49-95):
   - Remove `this->declare_parameter("left_tip", "left_wrist_yaw_link");` (currently line 54).
   - Remove `this->get_parameter("left_tip", left_tip_);` (currently line 92).

2. **Member variables** (private section, around lines 295-326):
   - Remove `KDL::Chain ... kdl_chain_left;` — replace `KDL::Chain kdl_chain_right, kdl_chain_left;` with `KDL::Chain kdl_chain_right;`.
   - Remove `ik_left` from the `std::shared_ptr<TRAC_IK::TRAC_IK> ik_right, ik_left;` declaration — leave `std::shared_ptr<TRAC_IK::TRAC_IK> ik_right;`.
   - Remove `fk_left_solver` from `std::shared_ptr<KDL::ChainFkSolverPos_recursive> fk_right_solver, fk_left_solver;` — leave `std::shared_ptr<KDL::ChainFkSolverPos_recursive> fk_right_solver;`.
   - Remove `std::string left_tip_ = "left_hand_palm_link";` (currently line 323).

3. **KDL chain extraction** (constructor, currently lines 115-120):
   - Remove the entire block:
     ```
     if (!kdl_tree.getChain(base_link_, left_tip_, kdl_chain_left)) {
         RCLCPP_FATAL(this->get_logger(), "Failed to extract KDL chain for left arm");
         rclcpp::shutdown();
         return;
     }
     printKDLChainInfo(kdl_chain_left, "left", this->get_logger());
     ```

4. **Joint limits arrays** (constructor, currently lines 130-162):
   - Remove the declarations `KDL::JntArray left_lower(...), left_upper(...);` (line 132).
   - Remove the entire `idx = 0; for (...left_chain segments...)` loop that fills `left_lower` and `left_upper` (currently lines 148-162).

5. **Left arm joint limits debug log** (constructor, currently lines 178-191):
   - Remove the entire `std::ostringstream left_chain_oss; ...` block including the trailing `RCLCPP_INFO` call.

6. **TRAC-IK initialization** (constructor, currently lines 197-198):
   - Remove `ik_left = std::make_shared<TRAC_IK::TRAC_IK>(shared_from_this(), kdl_chain_left, left_lower, left_upper, ik_timeout, ik_tol, solve_type);` (line 198).
   - Keep the `ik_right` initialization line (line 197) unchanged.

7. **Asserts** (constructor, currently lines 200-204):
   - Remove `assert(left_lower.rows() == kdl_chain_left.getNrOfJoints());` and `assert(left_upper.rows() == kdl_chain_left.getNrOfJoints());` (lines 203-204).

8. **FK solver init** (constructor, currently line 207):
   - Remove `fk_left_solver = std::make_shared<KDL::ChainFkSolverPos_recursive>(kdl_chain_left);`.

9. **Init INFO log lines mentioning left** (constructor, lines 218-232):
   - Replace `RCLCPP_INFO(... "Using base link: %s, right tip: %s, left tip: %s", ..., left_tip_.c_str());` with `RCLCPP_INFO(this->get_logger(), "Using base link: %s, right tip: %s", base_link_.c_str(), right_tip_.c_str());`.
   - Remove `RCLCPP_INFO(... "Left arm: base_link = %s, tip_link = %s", ..., left_tip_.c_str());` (line 228).
   - Remove `RCLCPP_INFO(... "Left arm KDL chain segments: %d", kdl_chain_left.getNrOfSegments());` (line 230).
   - Remove `RCLCPP_INFO(... "Left arm KDL chain joints: %d", kdl_chain_left.getNrOfJoints());` (line 232).
   - Keep the corresponding right-arm logs unchanged.

10. **KDL chain debug dump for left arm** (constructor, currently lines 265-286):
    - Remove the entire `std::ostringstream kdl_left_oss; kdl_left_oss << "Left arm KDL chain: "; for (...kdl_chain_left.getNrOfSegments()...) { ... } RCLCPP_INFO(...);` block.

11. **`use_right` y-axis selector** (in `goalPoseCallback`, currently around line 440):
    - Delete the line `bool use_right = pose_in_base.pose.position.y < 0.0;` entirely. After this deletion, the planner unconditionally plans for the right arm (D-02 satisfied).

12. **Replace all `use_right ? right : left` ternaries with right-only** (`goalPoseCallback`, around lines 442-599):
    - Line 442: `const KDL::Chain& chain = use_right ? kdl_chain_right : kdl_chain_left;` → `const KDL::Chain& chain = kdl_chain_right;`.
    - Line 533 (debug log inside the `RCLCPP_INFO` for "KDL/TRAC-IK base link..."): `use_right ? right_tip_.c_str() : left_tip_.c_str()` → `right_tip_.c_str()`.
    - Line 546: `auto& solver = use_right ? ik_right : ik_left;` → `auto& solver = ik_right;`.
    - Line 599: `auto& fk_solver = use_right ? fk_right_solver : fk_left_solver;` → `auto& fk_solver = fk_right_solver;`.

13. **`isInCollision` — drop the `bool use_right` parameter** (currently around line 803):
    - Change the signature from
      `bool isInCollision(const KDL::JntArray& joints, bool use_right, const std::vector<std::string>& skip_pairs = {}, const std::set<std::string>& planning_links = {})`
      to
      `bool isInCollision(const KDL::JntArray& joints, const std::vector<std::string>& skip_pairs = {}, const std::set<std::string>& planning_links = {})`.
    - Inside the body, replace `auto& fk_solver = use_right ? fk_right_solver : fk_left_solver;` (line 805) with `auto& fk_solver = fk_right_solver;`.
    - Inside the body, replace `auto& kdl_chain = use_right ? kdl_chain_right : kdl_chain_left;` (line 806) with `auto& kdl_chain = kdl_chain_right;`.
    - Update the single call site inside the OMPL state validity checker lambda (currently `isInCollision(joints, use_right, this->collision_skip_pairs_, planning_links)`) to drop the `use_right` argument: `isInCollision(joints, this->collision_skip_pairs_, planning_links)`.

Do NOT change the `printKDLChainInfo` helper or its right-arm call site (Plan 03 removes those as part of log cleanup). Do NOT change the collision filter `||` (Plan 02 owns that fix). Do NOT touch CMakeLists.txt or package.xml.

After all edits, the file must:
- Contain zero matches for `use_right`, `left_tip`, `kdl_chain_left`, `ik_left`, `fk_left_solver`, `left_lower`, `left_upper`, `left_chain_oss`, `kdl_left_oss`.
- Compile cleanly with `colcon build --packages-select unitree_g1_dex3_stack --cmake-args -DBUILD_IK_FCL_OMPL_PLANNER=ON`.
  </action>
  <verify>
grep -nE 'kdl_chain_left|ik_left|fk_left_solver|left_tip_|left_lower|left_upper|kdl_left_oss|left_chain_oss|left arm|Left arm|use_right' src/unitree_g1_dex3_stack-main/src/ik_fcl_ompl_planner.cpp
colcon build --packages-select unitree_g1_dex3_stack --cmake-args -DBUILD_IK_FCL_OMPL_PLANNER=ON 2>&1 | tail -30
  </verify>
  <acceptance_criteria>
    - `grep -nE 'kdl_chain_left|ik_left|fk_left_solver|left_tip_|left_lower|left_upper|kdl_left_oss|left_chain_oss' src/unitree_g1_dex3_stack-main/src/ik_fcl_ompl_planner.cpp` returns zero matches.
    - `grep -n 'use_right' src/unitree_g1_dex3_stack-main/src/ik_fcl_ompl_planner.cpp` returns zero matches.
    - `grep -n 'pose_in_base\.pose\.position\.y' src/unitree_g1_dex3_stack-main/src/ik_fcl_ompl_planner.cpp` returns zero matches.
    - `grep -nE 'Left arm:|Left arm KDL' src/unitree_g1_dex3_stack-main/src/ik_fcl_ompl_planner.cpp` returns zero matches.
    - `grep -c 'kdl_chain_right' src/unitree_g1_dex3_stack-main/src/ik_fcl_ompl_planner.cpp` returns at least 4 (still used in chain extraction, ternary replacements, isInCollision body).
    - `grep -c 'ik_right' src/unitree_g1_dex3_stack-main/src/ik_fcl_ompl_planner.cpp` returns at least 2.
    - `grep -c 'isInCollision' src/unitree_g1_dex3_stack-main/src/ik_fcl_ompl_planner.cpp` returns exactly 2 (definition + one call site).
    - The `isInCollision` definition signature contains exactly three parameters: `const KDL::JntArray& joints`, `const std::vector<std::string>& skip_pairs`, `const std::set<std::string>& planning_links`.
    - `colcon build --packages-select unitree_g1_dex3_stack --cmake-args -DBUILD_IK_FCL_OMPL_PLANNER=ON` completes with exit code 0 and no errors mentioning `kdl_chain_left`, `ik_left`, `fk_left_solver`, `left_tip`, or `use_right`.
  </acceptance_criteria>
  <done>The planner source compiles cleanly with all left-arm references and the y-axis arm selector removed; the planner now unconditionally targets the right arm.</done>
</task>

</tasks>

<verification>
- [ ] `grep -nE 'kdl_chain_left|ik_left|fk_left_solver|left_tip|left_lower|left_upper|use_right' src/unitree_g1_dex3_stack-main/src/ik_fcl_ompl_planner.cpp` returns zero matches.
- [ ] `grep -n 'left_tip' src/unitree_g1_dex3_stack-main/launch/planner.launch.py` returns zero matches.
- [ ] `grep -n 'collision_primitives' src/unitree_g1_dex3_stack-main/launch/robot.launch.py` returns at least one match in a `default_value=`.
- [ ] `colcon build --packages-select unitree_g1_dex3_stack --cmake-args -DBUILD_IK_FCL_OMPL_PLANNER=ON` exits 0 with no warnings about removed identifiers.
- [ ] Source file line count drops from ~903 to roughly ~810 (Plan 02 and Plan 03 will reduce it further).
</verification>

<success_criteria>
- All three tasks completed.
- Build succeeds with `BUILD_IK_FCL_OMPL_PLANNER=ON`.
- No left-arm tokens, no `use_right` selector, and no `left_tip` remain in planner source or planner launch file.
- Default URDF for `robot.launch.py` is the collision-primitives variant.
- The planner unconditionally targets the right arm regardless of goal y-coordinate.
</success_criteria>

<output>
After completion, create `.planning/phases/01-right-arm-only-planner/01-01-SUMMARY.md` documenting the LOC delta, any deviations, and any new questions for Plan 02.
</output>
