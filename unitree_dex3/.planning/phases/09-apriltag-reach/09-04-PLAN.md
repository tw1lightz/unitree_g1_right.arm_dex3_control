---
phase: 09-apriltag-reach
plan: 04
type: execute
wave: 2
depends_on:
  - 09-01
  - 09-02
  - 09-03
files_modified:
  - src/unitree_g1_dex3_stack-main/CMakeLists.txt
  - src/unitree_g1_dex3_stack-main/scripts/keyboard_trigger_node.py (delete)
  - src/unitree_g1_dex3_stack-main/package.xml
  - src/unitree_g1_dex3_stack-main/README.md
autonomous: true
requirements:
  - INTG-01
  - INTG-02
user_setup: []

must_haves:
  truths:
    - "CMakeLists.txt install(PROGRAMS ...) includes apriltag_goal_bridge.py and apriltag_reach_uat.py, excludes keyboard_trigger_node.py"
    - "keyboard_trigger_node.py is deleted from filesystem"
    - "package.xml has all required dependencies declared (same as before, no new deps needed)"
    - "README.md documents three launch entries with purpose table: apriltag_reach.launch.py (end-to-end), reach.launch.py (planner manual), apriltag.launch.py (detection debug)"
    - "README.md documents UAT command and G key trigger"
    - "README.md reminds user to pip install pupil-apriltags if not already installed"
  artifacts:
    - path: src/unitree_g1_dex3_stack-main/CMakeLists.txt
      provides: updated install entries for Phase 9 deliverables
      does_not_contain: keyboard_trigger_node.py
    - path: src/unitree_g1_dex3_stack-main/README.md
      provides: updated documentation with three-entry launch table
      contains: three-entry launch table, UAT command, G trigger key
  key_links:
    - from: "CMakeLists.txt install section"
      to: "scripts/apriltag_goal_bridge.py and scripts/apriltag_reach_uat.py"
      via: "install(PROGRAMS ... DESTINATION lib/${PROJECT_NAME})"
    - from: "README.md launch table"
      to: "launch/apriltag_reach.launch.py, launch/reach.launch.py, launch/apriltag.launch.py"
      via: "documentation cross-reference"
---

<objective>
Update build configuration and documentation: add bridge + UAT to CMakeLists install, delete keyboard_trigger_node.py, verify package.xml, and update README.md with three-entry launch table.

**Purpose:** These changes finalize Phase 9 by ensuring the new scripts are installed as ROS 2 executables, the YOLO-era keyboard_trigger_node is removed, and documentation reflects the new pipeline architecture with all three launch entry points.

**Output:**
- Modified `CMakeLists.txt` (install entries)
- Deleted `scripts/keyboard_trigger_node.py`
- Verified `package.xml` (no changes expected)
- Modified `README.md` (three-entry launch table + G key + UAT)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-apriltag-reach/09-CONTEXT.md
@.planning/phases/09-apriltag-reach/09-PATTERNS.md
@.planning/phases/09-apriltag-reach/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CMakeLists.txt install entries and delete keyboard_trigger_node.py</name>
  <files>
    src/unitree_g1_dex3_stack-main/CMakeLists.txt
    src/unitree_g1_dex3_stack-main/scripts/keyboard_trigger_node.py
  </files>
  <read_first>
    - src/unitree_g1_dex3_stack-main/CMakeLists.txt — current install(PROGRAMS ...) section at approximately lines 116-122, listing 4 scripts: tcp_torso_pose.py, keyboard_trigger_node.py, apriltag_detector_node.py, adaptive_orientation_ab.py
  </read_first>
  <action>
    Perform two operations:

    **1a. Edit CMakeLists.txt install(PROGRAMS ...) section:**

    Locate the `install(PROGRAMS` block (around line 116-122). The current content is:
    ```
    install(PROGRAMS
      scripts/tcp_torso_pose.py
      scripts/keyboard_trigger_node.py
      scripts/apriltag_detector_node.py
      scripts/adaptive_orientation_ab.py
      DESTINATION lib/${PROJECT_NAME}
    )
    ```

    Replace with:
    ```
    install(PROGRAMS
      scripts/tcp_torso_pose.py
      scripts/apriltag_detector_node.py
      scripts/adaptive_orientation_ab.py
      scripts/apriltag_goal_bridge.py
      scripts/apriltag_reach_uat.py
      DESTINATION lib/${PROJECT_NAME}
    )
    ```

    Changes:
    - REMOVE `scripts/keyboard_trigger_node.py` (D-04: YOLO-era legacy, being deleted)
    - ADD `scripts/apriltag_goal_bridge.py` (new bridge node from Plan 01)
    - ADD `scripts/apriltag_reach_uat.py` (new UAT harness from Plan 03)
    - Keep alphabetical order (within new set)
    - Do NOT change anything else in CMakeLists.txt (no other sections touched)

    **1b. Delete keyboard_trigger_node.py:**

    Remove the file `scripts/keyboard_trigger_node.py` from the filesystem (D-04: YOLO-era legacy trigger node, fully replaced by `apriltag_goal_bridge.py`). Use `git rm` (preferred to also stage the deletion) or `rm` followed by `git add` for the deletion.

    Verify the file no longer exists:
    ```bash
    test ! -f src/unitree_g1_dex3_stack-main/scripts/keyboard_trigger_node.py
    ```
  </action>
  <verify>
    <automated>
      # Verify CMakeLists changes
      python3 -c "
import sys
content = open('src/unitree_g1_dex3_stack-main/CMakeLists.txt').read()
# Must contain new scripts
assert 'apriltag_goal_bridge.py' in content, 'Missing apriltag_goal_bridge.py in install'
assert 'apriltag_reach_uat.py' in content, 'Missing apriltag_reach_uat.py in install'
# Must NOT contain deleted script
assert 'keyboard_trigger_node.py' not in content, 'keyboard_trigger_node.py still in install'
print('CMakeLists.txt OK: bridge+UAT added, keyboard_trigger removed')
sys.exit(0)
"
      # Verify file deletion
      test ! -f src/unitree_g1_dex3_stack-main/scripts/keyboard_trigger_node.py && echo "DELETED OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - CMakeLists.txt install(PROGRAMS ...) contains `scripts/apriltag_goal_bridge.py` and `scripts/apriltag_reach_uat.py`
    - CMakeLists.txt does NOT contain `scripts/keyboard_trigger_node.py`
    - `scripts/keyboard_trigger_node.py` no longer exists on filesystem
    - No other sections of CMakeLists.txt were modified
  </acceptance_criteria>
  <done>
    CMakeLists.txt updated with new install entries; keyboard_trigger_node.py deleted from both CMakeLists and filesystem.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify package.xml and update README.md</name>
  <files>
    src/unitree_g1_dex3_stack-main/package.xml
    src/unitree_g1_dex3_stack-main/README.md
  </files>
  <read_first>
    - src/unitree_g1_dex3_stack-main/package.xml — current dependency list (rclpy, geometry_msgs, trajectory_msgs, tf2_ros, sensor_msgs already declared)
    - src/unitree_g1_dex3_stack-main/README.md — current content (YOLO-era references, parameter table, quick start)
  </read_first>
  <action>
    Perform two operations:

    **2a. Verify package.xml:**

    Read the current `package.xml`. Phase 9 bridge node and UAT harness use only: rclpy, geometry_msgs, trajectory_msgs, tf2_ros, sensor_msgs, Python stdlib (os, select, termios, tty, collections, math), numpy (optional, used in UAT), and pinocchio (used in UAT for FK).

    All of rclpy, geometry_msgs, trajectory_msgs, tf2_ros, sensor_msgs should already be declared as `<exec_depend>` or `<depend>` from previous phases. If any are missing, add them. If all present, make NO changes to package.xml.

    Pinocchio and numpy are typically pip/conda-managed (not ROS packages), so they should NOT be added to package.xml (matching read_tcp_pose.py's approach — it also uses pinocchio via pip without a package.xml entry).

    **2b. Update README.md:**

    Append (or add, if README is short) a new section in the README. If the README already has sections, insert after the quick-start section. The content:

    ```
    ## 三条启动入口

    Phase 9 (AprilTag 端到端) 后，共有三条 launch 入口，用途不同：

    | Launch | 用途 |
    |--------|------|
    | `apriltag_reach.launch.py` | **端到端流水线**：AprilTag 检测 → bridge 缓存 → G 键触发 → planner (自适应位姿) → executor。最常用入口。 |
    | `reach.launch.py` | **Planner 手动测试**：仅启动 robot + planner + executor，不启动检测。通过 `ros2 topic pub /goal_pose` 手动发布目标位姿。 |
    | `apriltag.launch.py` | **检测独立调试**：仅启动 robot + RealSense + AprilTag 检测节点 (含 OpenCV 可视化)，不启动 planner/executor。用于调试 tag 识别、摆位、decision_margin 参数。 |

    ### 使用方法

    1. **端到端**：`ros2 launch unitree_g1_dex3_stack apriltag_reach.launch.py`
       - 启动后等待约 3 秒让各节点就绪
       - 将 AprilTag 摆在目标位置，观察 OpenCV 窗口确认检测
       - 在终端按 **G** 键触发（不是 Enter，直接按 G）
       - Bridge 日志显示 `|target-shoulder|` 距离和 `publishing /goal_pose`

    2. **Planner 手动测试**：`ros2 launch unitree_g1_dex3_stack reach.launch.py`
       - 然后另开终端：`ros2 topic pub /goal_pose geometry_msgs/PoseStamped "{header: {frame_id: 'torso_link'}, pose: {position: {x: 0.4, y: -0.2, z: 0.0}, orientation: {w: 1.0}}}"`

    3. **检测独立调试**：`ros2 launch unitree_g1_dex3_stack apriltag.launch.py imshow:=true`
       - 带可视化窗口调试 tag 识别；按 q 关闭窗口或退出

    ### 端到端 UAT

    ```bash
    ros2 run unitree_g1_dex3_stack apriltag_reach_uat.py
    ```

    UAT harness 逐一测试 4 个 tabletop 目标点，通过 KDL FK 测量 TCP 实际位置与目标位置的误差，输出每个点的 PASS/FAIL 及总 PASS_COUNT。要求 4/4 全部通过（误差 ≤ 3 cm）。

    操作步骤：
    1. 启动 `apriltag_reach.launch.py`
    2. 另开终端运行 `apriltag_reach_uat.py`
    3. UAT 提示 "Place tag at point N" → 摆放 AprilTag 到指定位置 → 按 G → 等待测量
    4. 重复 4 次，确认全部 PASS

    ### 前提条件

    - **pupil-apriltags**（Phase 7）：`pip install pupil-apriltags`
    - **Realsense SDK**：`sudo apt install ros-humble-realsense2-camera`
    - **colcon build**：`colcon build --cmake-args -DBUILD_IK_FCL_OMPL_PLANNER=ON`
    - **conda environment**：启动前 `conda activate grab`（Perception 节点在 conda 环境下运行）
    ```

    If the README currently has a "三条启动入口" or similar section, replace it with the above. If the README has YOLO-era references that are misleading, update them. Otherwise, append the new section at the end.

    **What to avoid:**
    - Do NOT remove existing sections of README that are still accurate (environment setup, parameter tables, etc.)
    - Do NOT add instructions for deleted files (keyboard_trigger_node)
    - Do NOT add speculative content about future features (ORI-02, TAG-05)
  </action>
  <verify>
    <automated>
      # Verify package.xml changes
      python3 -c "
import sys
content = open('src/unitree_g1_dex3_stack-main/package.xml').read()
# Check that key deps already exist
deps = ['rclpy', 'geometry_msgs', 'trajectory_msgs', 'tf2_ros', 'sensor_msgs']
for d in deps:
    assert d in content, f'Missing package dep: {d}'
print('package.xml OK: all required deps present')
sys.exit(0)
"
      # Verify README changes
      python3 -c "
import sys
content = open('src/unitree_g1_dex3_stack-main/README.md').read()
# Must have three-entry launch table
assert 'apriltag_reach.launch.py' in content, 'Missing apriltag_reach.launch.py in README'
assert 'reach.launch.py' in content, 'Missing reach.launch.py in README'
assert 'apriltag.launch.py' in content, 'Missing apriltag.launch.py in README'
# Must have G key documentation
assert 'G' in content and ('触发' in content or 'press' in content or '按 G' in content), 'Missing G key trigger documentation'
# Must have UAT command
assert 'apriltag_reach_uat.py' in content or 'apriltag_reach_uat' in content, 'Missing UAT command in README'
# Must have pip reminder
assert 'pupil-apriltags' in content, 'Missing pupil-apriltags reminder in README'
print('README.md OK: launch table, G key, UAT, pip reminder all present')
sys.exit(0)
"
    </automated>
  </verify>
  <acceptance_criteria>
    - package.xml has all required dependencies (rclpy, geometry_msgs, trajectory_msgs, tf2_ros, sensor_msgs)
    - No new dependencies added to package.xml (pinocchio/numpy are pip/conda-managed)
    - README.md contains:
      - Three-entry launch table (apriltag_reach, reach, apriltag) with Chinese description column
      - G key trigger documentation
      - UAT command (`ros2 run ... apriltag_reach_uat.py`)
      - pupil-apriltags pip reminder
    - No YOLO-era or keyboard_trigger_node references remain in README
  </acceptance_criteria>
  <done>
    package.xml verified (no new deps needed) and README.md updated with full documentation of all three launch entries, G key trigger, UAT command, and pupil-apriltags prerequisite.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CMakeLists.txt | Build configuration; malformed install entries cause missing executables at runtime |
| README.md | Documentation; inaccurate instructions cause user error |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-11 | Tampering | CMakeLists.txt install | mitigate | Install entries are explicit file paths; bridge + UAT added, keyboard_trigger removed. Verified by automated grep assertions |
| T-09-12 | Tampering | README.md accuracy | mitigate | Three-entry table cross-references actual launch file names; G key trigger documentation matches bridge D-02; UAT command matches Plan 03 harness name |
| T-09-SC | Tampering | package installs | accept | No new packages; existing deps only (rclpy, geometry_msgs, trajectory_msgs, tf2_ros, sensor_msgs which are ROS 2 standard packages) |
</threat_model>

<verification>
Phase-level verification for Plan 04 artifacts:
- [ ] CMakeLists.txt install section updated (bridge + UAT added, keyboard_trigger removed)
- [ ] keyboard_trigger_node.py deleted from filesystem
- [ ] package.xml has no missing deps, no unneeded additions
- [ ] README.md has three-entry launch table with descriptions
- [ ] README.md documents G key trigger and UAT command
- [ ] README.md has pupil-apriltags pip reminder
</verification>

<success_criteria>
Build configuration is updated for Phase 9 deliverables, legacy trigger file is removed, and documentation accurately reflects the three pipeline entry points with trigger key and UAT instructions.
</success_criteria>

<output>
Create `.planning/phases/09-apriltag-reach/09-04-SUMMARY.md` when done
</output>
