---
phase: 08-adaptive-orientation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/unitree_g1_dex3_stack-main/src/ik_fcl_ompl_planner.cpp
  - src/unitree_g1_dex3_stack-main/launch/planner.launch.py
autonomous: true
requirements: [ORI-01]

must_haves:
  truths:
    - "On planner startup, an INFO log line reports the right shoulder reference point in torso_link with xyz within 1e-3 m of (0.0040, -0.1002, 0.2478) — the URDF rest position of right_shoulder_pitch_link in torso_link. Implements D-05 (shoulder ref = right_shoulder_pitch_link origin) and D-06 (compute via URDF/KDL, no runtime TF lookup); the log line itself satisfies the startup-confirmation form of D-12."
    - "On planner startup, an INFO log line reports `adaptive_orientation_enabled = true` (default) or `= false` (when launch arg flips it). Implements D-10 (ROS parameter named adaptive_orientation_enabled, default true)."
    - "When adaptive_orientation_enabled=true and a /goal_pose with arbitrary orientation arrives at a tabletop position, the planner emits exactly one `Adaptive orientation: target=[…] shoulder=[…] dir=[…] q=[…]` INFO line per goal AND publishes /joint_trajectory_targets. Implements D-01 (right_tcp_link local +X approach axis points from shoulder toward target), D-02 (torso +Z up reference for roll stability), D-03 (torso +Y fallback when near-parallel to +Z), D-04 (single deterministic orientation per goal), D-09 (default overwrite of incoming orientation), and D-12 (per-goal log of target/shoulder/direction/quaternion)."
    - "When adaptive_orientation_enabled=true and the goal position is within 0.05 m of the cached shoulder, the planner emits an `RCLCPP_ERROR` line containing `within 0.05 m of right shoulder` and does NOT publish /joint_trajectory_targets for that goal. Implements D-08 (reject too-close target with clear error, no trajectory)."
    - "When adaptive_orientation_enabled=false, the planner uses the incoming /goal_pose.pose.orientation byte-for-byte unchanged — no Adaptive orientation log line is emitted, and the planner produces the same trajectory it would have produced before Phase 8 for the same input. Implements D-11 (when false, preserve old planner behavior exactly)."
    - "Phase 8 adds exactly one ROS parameter (`adaptive_orientation_enabled`); the shoulder link name `right_shoulder_pitch_link` is hardcoded as a string literal in the FK call path and does NOT become a configurable ROS parameter. Implements D-07 (shoulder link name hardcoded, no new ROS parameter for the shoulder link)."
  artifacts:
    - path: src/unitree_g1_dex3_stack-main/src/ik_fcl_ompl_planner.cpp
      provides: "Adaptive orientation member fields, parameter declaration, computeAdaptiveOrientation private method, and goalPoseCallback splice point"
      contains: "computeAdaptiveOrientation"
    - path: src/unitree_g1_dex3_stack-main/launch/planner.launch.py
      provides: "adaptive_orientation_enabled launch argument wired into the parameters dict"
      contains: "adaptive_orientation_enabled"
  key_links:
    - from: "computeAdaptiveOrientation()"
      to: "right_shoulder_pos_in_base_"
      via: "cached shoulder origin computed once at init via FK"
      pattern: "right_shoulder_pos_in_base_"
    - from: "goalPoseCallback()"
      to: "computeAdaptiveOrientation()"
      via: "pose_in_base.pose.orientation overwrite when adaptive_orientation_enabled_=true"
      pattern: "computeAdaptiveOrientation\\("
    - from: "planner.launch.py adaptive_orientation_enabled arg"
      to: "ik_fcl_ompl_planner adaptive_orientation_enabled ROS parameter"
      via: "OpaqueFunction parameters dict"
      pattern: "'adaptive_orientation_enabled'"
---

<objective>
Implement adaptive end-effector orientation inside `ik_fcl_ompl_planner` so the planner derives the TCP +X approach axis from the shoulder→target direction (per CONTEXT.md D-01..D-12) and exposes the feature behind a launch-time toggle. Position the change as a surgical, file-local edit consistent with the existing `goalPoseCallback` flow.

Purpose: ORI-01 — fixed end-effector poses are causing IK/OMPL failures. Replacing the upstream-supplied orientation with a deterministic shoulder-to-target +X approach gives TRAC-IK a feasible target on every tabletop goal, which Phase 8's A/B harness (Plan 02) will quantify.

Output:
- A modified `ik_fcl_ompl_planner.cpp` with: two new private members, one parameter declaration, a shoulder-origin cache initialized via KDL FK, a `computeAdaptiveOrientation` private method, and a splice block in `goalPoseCallback` that overwrites `pose_in_base.pose.orientation` when the toggle is on.
- A modified `planner.launch.py` exposing `adaptive_orientation_enabled` (default `true`) as a launch arg flowing into the planner's ROS parameters dict.
- No changes to any other file. No new headers. No new dependencies. No new ROS topics or messages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-adaptive-orientation/08-CONTEXT.md
@.planning/phases/08-adaptive-orientation/08-RESEARCH.md
@.planning/phases/08-adaptive-orientation/08-PATTERNS.md
@.planning/phases/08-adaptive-orientation/08-VALIDATION.md
@src/unitree_g1_dex3_stack-main/src/ik_fcl_ompl_planner.cpp
@src/unitree_g1_dex3_stack-main/launch/planner.launch.py
@src/unitree_g1_dex3_stack-main/robots/g1_description/g1_29dof_lock_waist_with_hand_rev_1_0.urdf

<interfaces>
<!-- Existing planner private API the new code latches into. Extracted from ik_fcl_ompl_planner.cpp. -->

Existing private members of `IKFCLPlannerNode` (already declared, do NOT redeclare):
```cpp
KDL::Tree kdl_tree;
KDL::Chain kdl_chain_right;                  // built torso_link → right_tcp_link
std::shared_ptr<KDL::ChainFkSolverPos_recursive> fk_right_solver;
std::string base_link_;                      // "torso_link" by default
std::string right_tip_;                      // "right_tcp_link" by default
tf2_ros::Buffer tf_buffer_;
```

Existing FK call signature already used in `isInCollision()`:
```cpp
fk_right_solver->JntToCart(joints, out_frame, segmentNr);  // KDL is 1-based on segmentNr
```

`KDL::ChainFkSolverPos_recursive::JntToCart(q, out, 1)` returns the frame at the end of segment 0 — for `kdl_chain_right` (root = torso_link), segment 0 is the segment created by `right_shoulder_pitch_joint`, so the returned frame's position is the `right_shoulder_pitch_link` origin in `torso_link`. With all joints zero this yields the URDF rest-pose origin, which is invariant of joint state because revolute-joint angle does not move the child link's origin — only its rotation.

URDF ground-truth (from `robots/g1_description/g1_29dof_lock_waist_with_hand_rev_1_0.urdf`):
```xml
<joint name="right_shoulder_pitch_joint" type="revolute">
  <origin xyz="0.0039563 -0.10021 0.24778" rpy="-0.27931 5.4949E-05 0.00019159" />
  <parent link="torso_link" />
  <child link="right_shoulder_pitch_link" />
  <axis xyz="0 1 0" />
  <limit lower="-3.0892" upper="2.6704" effort="25" velocity="37" />
</joint>
```

Acceptance threshold for shoulder-cache log: position within 1e-3 m of (0.0040, -0.1002, 0.2478).

KDL math API used by `computeAdaptiveOrientation`:
- `KDL::Vector` — 3-vector with `Norm()`, `Normalize()`, `x()`/`y()`/`z()`, operator overloads `+`, `-`, `*` (scalar mul AND cross product when both operands are Vector), and `KDL::dot(a, b)`.
- `KDL::Rotation(const Vector&, const Vector&, const Vector&)` — column-vector ctor (3 columns of the rotation matrix). If unavailable on this KDL build, fall back to the 9-double ctor `KDL::Rotation(Xx, Yx, Zx, Xy, Yy, Zy, Xz, Yz, Zz)` (column-major).
- `KDL::Rotation::GetQuaternion(double& x, double& y, double& z, double& w)`.
</interfaces>

<thresholds>
<!-- Numeric thresholds chosen during research. Plans MUST use these exact constants. -->

```cpp
constexpr double kMinTargetDistance     = 0.05;   // meters; D-08 reject threshold (PIT-03)
constexpr double kParallelDotThreshold  = 0.95;   // dimensionless; D-03 fallback trigger (PIT-02)
```

Both constants are local `constexpr` inside `computeAdaptiveOrientation` — they MUST NOT be promoted to ROS parameters (CLAUDE.md §2 simplicity; D-07 forbids extra parameters).
</thresholds>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement adaptive orientation in ik_fcl_ompl_planner.cpp</name>
  <files>src/unitree_g1_dex3_stack-main/src/ik_fcl_ompl_planner.cpp</files>

  <read_first>
    src/unitree_g1_dex3_stack-main/src/ik_fcl_ompl_planner.cpp (whole file — surgical edit, must see existing init() and goalPoseCallback() flow before modifying)
    .planning/phases/08-adaptive-orientation/08-CONTEXT.md (D-01..D-12 must be honored verbatim)
    .planning/phases/08-adaptive-orientation/08-RESEARCH.md (Examples E-1, E-2, E-3 — these are reference implementations; the executor uses them as the contract)
    .planning/phases/08-adaptive-orientation/08-PATTERNS.md (trailing-underscore convention, RCLCPP_INFO/ERROR pattern, existing PoseStamped mutation pattern)
    .planning/codebase/CONVENTIONS.md (C++17, -Wall -Wextra -Wpedantic, RCLCPP_FATAL+shutdown for boot errors)
  </read_first>

  <action>
Add a private enum, two private members, one parameter declaration with INFO log, a shoulder-origin cache, one private method, and one splice block — all inside `class IKFCLPlannerNode` in the same single source file. No new headers, no new translation units, no changes to public API. Per D-09, mutate `pose_in_base.pose.orientation` in place at the splice point so all downstream code (target_frame, IK seed retry, OMPL setup) consumes the adaptive value.

(1) **Members.** In the existing `private:` block (the one beginning with `urdf::Model urdf_model;`), add two members near the other state fields, using trailing-underscore naming:

`KDL::Vector right_shoulder_pos_in_base_;`
`bool adaptive_orientation_enabled_ = true;`

(2) **Local enum.** Inside `class IKFCLPlannerNode` `private:` section, declare a small status enum scoped to the helper:

`enum class AdaptiveOrientationStatus { OK, TARGET_TOO_CLOSE_TO_SHOULDER };`

(3) **Parameter wiring.** In `init()`, near the existing `tcp_offset_x` declare/get block (inside the same braced scope is acceptable; right after that block is also acceptable), add:

`this->declare_parameter("adaptive_orientation_enabled", true);`
`this->get_parameter("adaptive_orientation_enabled", adaptive_orientation_enabled_);`

Then a single `RCLCPP_INFO` line: `adaptive_orientation_enabled = %s` printing `true`/`false` based on the bool. Mirrors the existing "Using base link" / "Planning timeout" startup INFO style.

(4) **Shoulder cache.** In `init()`, after the line `fk_right_solver = std::make_shared<KDL::ChainFkSolverPos_recursive>(kdl_chain_right);`, compute and cache the shoulder origin via FK on segment 1 (KDL is 1-based) with all-zero joints. On FK failure, call `RCLCPP_FATAL` with a clear message, then `rclcpp::shutdown(); return;` — matches the existing `RCLCPP_FATAL` boot-stop pattern in this file. On success, `RCLCPP_INFO` the cached xyz with format `Right shoulder reference point in '%s': [%.4f, %.4f, %.4f]` using `base_link_.c_str()` and the three components.

(5) **Helper method.** Add a `private:` member function `AdaptiveOrientationStatus computeAdaptiveOrientation(const KDL::Vector& target_in_base, double& out_qx, double& out_qy, double& out_qz, double& out_qw, KDL::Vector& out_dir_normalized) const;`. Implement the look-at orthonormal basis from RESEARCH Example E-1: subtract shoulder, reject if `Norm() < kMinTargetDistance`, normalize, choose up = `+Z_torso` and switch to `+Y_torso` when `|dot(x_axis, up)| > kParallelDotThreshold`, compute `y_axis = up * x_axis` (KDL `*` on two `Vector` is cross product), `Normalize()`, then `z_axis = x_axis * y_axis`. Build `KDL::Rotation` from the three column vectors (use the 3-Vector ctor; if the build refuses it on this KDL distribution, fall back to the 9-double column-major ctor). Call `R.GetQuaternion(out_qx, out_qy, out_qz, out_qw)`. Set `out_dir_normalized = x_axis`. Return `OK`. Use only `KDL::Vector` types in the body — do NOT mix in `Eigen::Vector3d` (PIT-04). The constants `kMinTargetDistance = 0.05` and `kParallelDotThreshold = 0.95` are `constexpr double` locals inside the method body; they MUST NOT become ROS parameters (D-07, CLAUDE.md §2).

(6) **Splice in `goalPoseCallback`.** Insert the adaptive-orientation block IMMEDIATELY after the `if (pose->header.frame_id.empty() || …) { pose_in_base = *pose; … } else { pose_in_base = tf_buffer_.transform(...); … }` block AND BEFORE the `// Dynamically generate planning_joints from KDL chain` comment (and the `const KDL::Chain& chain = kdl_chain_right;` line that follows it). Wrap the entire block in `if (adaptive_orientation_enabled_) { … }` per D-09/D-11 — when the flag is `false`, this block is skipped entirely and the existing flow runs bit-exactly the same as pre-Phase-8 (D-11). Inside the if-block: build `KDL::Vector target_in_base` from the three `pose_in_base.pose.position.{x,y,z}` doubles; call `computeAdaptiveOrientation`; on `TARGET_TOO_CLOSE_TO_SHOULDER` emit an `RCLCPP_ERROR` containing the literal substring `within 0.05 m of right shoulder` (acceptance criterion 5 in `08-VALIDATION.md` greps for this) followed by target xyz, then `return;` (matches existing IK-failure early-return pattern); on `OK` overwrite the four `pose_in_base.pose.orientation.{x,y,z,w}` fields with the returned quaternion components, then emit one `RCLCPP_INFO` line with the format from RESEARCH Example E-2 — target xyz at `%.3f`, shoulder xyz at `%.3f`, direction at `%.3f`, quaternion at `%.4f`, all on a single log line so the planner's downstream "Transformed goal_pose" / "TRAC-IK result" lines remain visually paired with the per-goal lifecycle. Add an inline `// Phase 8: intentional in-place mutation per D-09` comment at the splice site (PIT-05 mitigation).

Surgical-change constraint per CLAUDE.md §3: do NOT touch the IK seed retry loop, the OMPL setup, the trajectory smoothing, the auto-fix block, or any logging/comments outside the splice site. Match existing C++17 style — `Wall/Wextra/Wpedantic` clean, no warnings introduced.
  </action>

  <acceptance_criteria>
    Source assertions (executable via grep on the modified file):
    - `grep -c 'KDL::Vector right_shoulder_pos_in_base_' src/unitree_g1_dex3_stack-main/src/ik_fcl_ompl_planner.cpp` returns 1.
    - `grep -c 'bool adaptive_orientation_enabled_' src/unitree_g1_dex3_stack-main/src/ik_fcl_ompl_planner.cpp` returns 1.
    - `grep -c 'declare_parameter("adaptive_orientation_enabled"' src/unitree_g1_dex3_stack-main/src/ik_fcl_ompl_planner.cpp` returns 1.
    - `grep -c 'computeAdaptiveOrientation' src/unitree_g1_dex3_stack-main/src/ik_fcl_ompl_planner.cpp` returns ≥ 3 (declaration + definition + call site, possibly + enum-using-line).
    - `grep -c 'AdaptiveOrientationStatus::OK\|AdaptiveOrientationStatus::TARGET_TOO_CLOSE_TO_SHOULDER' src/unitree_g1_dex3_stack-main/src/ik_fcl_ompl_planner.cpp` returns ≥ 2.
    - `grep -c 'kMinTargetDistance\b\|kParallelDotThreshold\b' src/unitree_g1_dex3_stack-main/src/ik_fcl_ompl_planner.cpp` returns ≥ 2 (both constants present).
    - The literal string `within 0.05 m of right shoulder` appears in the file (used by the RCLCPP_ERROR for D-08).
    - The literal string `Adaptive orientation: target=` appears in the file (used by the RCLCPP_INFO for D-12).

    Build assertion:
    - `colcon build --packages-select unitree_g1_dex3_stack --cmake-args -DBUILD_IK_FCL_OMPL_PLANNER=ON` exits 0.
    - The build emits no new warnings beyond the pre-Phase-8 baseline (`-Wall -Wextra -Wpedantic` clean).
    - `nm -C build/unitree_g1_dex3_stack/CMakeFiles/ik_fcl_ompl_planner.dir/src/ik_fcl_ompl_planner.cpp.o | grep -c computeAdaptiveOrientation` returns ≥ 1 (symbol present after compile).

    Behavior assertions (deferred to runtime in Plan 02; this task's done state is the source + build assertions above. Plan 02's harness exercises the runtime behaviors).
  </acceptance_criteria>

  <verify>
    <automated>colcon build --packages-select unitree_g1_dex3_stack --cmake-args -DBUILD_IK_FCL_OMPL_PLANNER=ON 2>&1 | tail -20 ; ls -la build/unitree_g1_dex3_stack/ik_fcl_ompl_planner</automated>
  </verify>

  <done>
The modified planner compiles cleanly with `BUILD_IK_FCL_OMPL_PLANNER=ON`. All grep-based source assertions in `<acceptance_criteria>` pass. The compiled object file contains the `computeAdaptiveOrientation` symbol. No public API change, no header file added, no other source file touched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire adaptive_orientation_enabled launch arg in planner.launch.py</name>
  <files>src/unitree_g1_dex3_stack-main/launch/planner.launch.py</files>

  <read_first>
    src/unitree_g1_dex3_stack-main/launch/planner.launch.py (whole file — small, see existing arg pattern before adding the new line)
    .planning/phases/08-adaptive-orientation/08-PATTERNS.md (the existing arg-declaration / perform-coerce / parameters-dict triple is the contract)
    .planning/phases/08-adaptive-orientation/08-RESEARCH.md (Example E-4 is the reference implementation)
  </read_first>

  <action>
Add exactly one new `DeclareLaunchArgument` to the `args` list, exactly one new perform-and-coerce line in `launch_setup`, and exactly one new key in the `parameters` dict — the same triple already used for `tcp_offset_x`, `planner_type`, and the other existing parameters.

(1) In the `args = [ … ]` list (after the existing entries, before `DeclareLaunchArgument('collision_skip_pairs', …)` is fine; placing it last is also fine), add: `DeclareLaunchArgument('adaptive_orientation_enabled', default_value='true')`.

(2) In `launch_setup(context, *args, **kwargs)`, immediately after the existing `planner_type = str(LaunchConfiguration('planner_type').perform(context))` line, add the bool-coercion line: `adaptive_orientation_enabled = LaunchConfiguration('adaptive_orientation_enabled').perform(context).lower() == 'true'`. Do NOT use Python's built-in `bool(...)` — `bool('false')` evaluates to `True`, which would silently break the `:=false` flow.

(3) In the `parameters = { … }` dict literal, add the key-value pair: `'adaptive_orientation_enabled': adaptive_orientation_enabled,`. Do not add an `if`-guard around it — unlike `collision_skip_pairs`, this parameter always has a value.

Do NOT change anything else — not the `arguments=['--ros-args', '--log-level', 'trac_ik.ros.trac_ik:=DEBUG']` block, not the `output='screen'`, not the existing args' default values, not the `collision_skip_pairs` parsing logic.
  </action>

  <acceptance_criteria>
    Source assertions (executable via grep):
    - `grep -c "DeclareLaunchArgument('adaptive_orientation_enabled', default_value='true')" src/unitree_g1_dex3_stack-main/launch/planner.launch.py` returns 1.
    - `grep -c "LaunchConfiguration('adaptive_orientation_enabled').perform(context).lower() == 'true'" src/unitree_g1_dex3_stack-main/launch/planner.launch.py` returns 1.
    - `grep -c "'adaptive_orientation_enabled': adaptive_orientation_enabled" src/unitree_g1_dex3_stack-main/launch/planner.launch.py` returns 1.

    Syntactic assertion:
    - `python3 -m py_compile src/unitree_g1_dex3_stack-main/launch/planner.launch.py` exits 0.

    Launch-time assertion (after Task 1 has shipped and `colcon build` has succeeded; verified manually or by Plan 02's harness):
    - `ros2 launch unitree_g1_dex3_stack planner.launch.py adaptive_orientation_enabled:=false 2>&1 | head -50 | grep -c 'adaptive_orientation_enabled = false'` returns ≥ 1 (planner reads the flipped value).
    - The default launch (no args overridden) shows `adaptive_orientation_enabled = true` in the planner's startup INFO output.
  </acceptance_criteria>

  <verify>
    <automated>python3 -m py_compile src/unitree_g1_dex3_stack-main/launch/planner.launch.py && grep -c "adaptive_orientation_enabled" src/unitree_g1_dex3_stack-main/launch/planner.launch.py</automated>
  </verify>

  <done>
The launch file compiles syntactically. The three grep counts above each return 1. Launching the planner with `adaptive_orientation_enabled:=false` flips the parameter; the default launch leaves it `true`. No other launch-arg defaults change.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ROS DDS / DOMAIN → planner `/goal_pose` subscriber | Any process on the same ROS_DOMAIN can publish PoseStamped messages to `/goal_pose`. This boundary already existed pre-Phase-8; Phase 8 does not widen or narrow it. |
| ROS parameter server → planner | Launch-time parameter wiring. `adaptive_orientation_enabled` is read once at startup. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-08-01 | Tampering | `/goal_pose.pose.orientation` field — upstream may publish a fixed/default quaternion that, when used as TRAC-IK target orientation, lies in an unreachable region of SE(3). | mitigate | D-09 default `adaptive_orientation_enabled=true` overwrites the upstream-supplied orientation with a deterministic shoulder-to-target orientation, neutralising the impact of an upstream-supplied bad quaternion. This is precisely the requirement ORI-01 was created to address. |
| T-08-02 | Denial of Service | A malicious or buggy publisher could send `/goal_pose` positions extremely close to the cached shoulder origin, repeatedly pushing the planner into the FK / direction-vector code path with degenerate inputs. | mitigate | D-08 reject path: if `‖target − shoulder‖ < 0.05 m`, the planner emits `RCLCPP_ERROR`, returns early, and does NOT publish a trajectory. Bounded compute, no NaN propagation, no half-baked `/joint_trajectory_targets`. |
| T-08-03 | Information Disclosure | The Phase 8 INFO log line (D-12) exposes target xyz, shoulder xyz, direction, and quaternion to any process reading the ROS log. | accept | The G1 lab environment does not treat right-arm goal positions as sensitive; existing planner already logs `goal_pose` xyz on the same ROS log channel. No new disclosure surface. |
| T-08-04 | Tampering | ROS parameter `adaptive_orientation_enabled` could be set to `false` by a misconfigured launch, silently restoring pre-Phase-8 behavior. | accept | This is the explicitly designed A/B mode (D-10/D-11). The startup INFO `adaptive_orientation_enabled = false` line makes the configuration visible in operator logs, fulfilling the operational-visibility need. |
| T-08-SC | Tampering | npm/pip/cargo supply chain. | N/A | Phase 8 installs zero new packages — no `pip install`, no `apt install`, no `<depend>` additions, no `find_package` additions. RESEARCH.md `## Package Legitimacy Audit` confirms nothing to audit. |
</threat_model>

<verification>
Plan 01 is complete when:
1. The modified planner builds clean with `-DBUILD_IK_FCL_OMPL_PLANNER=ON`.
2. All grep-based source assertions in both tasks pass.
3. The launch file syntactically compiles via `python3 -m py_compile`.
4. A smoke-launch of `ros2 launch unitree_g1_dex3_stack planner.launch.py` shows the two new INFO log lines: shoulder reference point with xyz ≈ (0.0040, -0.1002, 0.2478) ±1e-3 m, and `adaptive_orientation_enabled = true`. Flipping `:=false` shows `adaptive_orientation_enabled = false`.
5. Publishing one tabletop goal (e.g., position `(0.40, -0.20, 0.00)` in `torso_link`, any orientation) causes the planner to emit one `Adaptive orientation: target=… shoulder=… dir=… q=…` INFO line and proceed into the existing IK/OMPL flow. The downstream behavior is Plan 02's responsibility to systematically verify.

Runtime full-coverage verification (per-target tabletop set, A/B comparison, D-08 reject-path) is delivered by Plan 02. Plan 01 ships the implementation; Plan 02 ships the harness that exercises it.
</verification>

<success_criteria>
- ORI-01 implementation contract is in `ik_fcl_ompl_planner.cpp` and `planner.launch.py`.
- D-01..D-12 each map to a concrete code change visible to the executor and verifiable by grep or runtime log.
- `colcon build --packages-select unitree_g1_dex3_stack --cmake-args -DBUILD_IK_FCL_OMPL_PLANNER=ON` rc=0 with no new warnings.
- No file outside the two listed in `files_modified` is touched.
- No new ROS topic, message, parameter (other than `adaptive_orientation_enabled`), or dependency.
- The `:=false` regression path bit-exactly preserves pre-Phase-8 behavior (D-11).
</success_criteria>

<output>
Create `.planning/phases/08-adaptive-orientation/08-01-SUMMARY.md` when done.
</output>

<decision_coverage>
## Decision Coverage Map

This plan delivers the implementation half of CONTEXT.md decisions. Each item below cites the decision ID followed by where the plan addresses it.

- **D-01** (TCP +X approach axis) — Task 1 step 5: `x_axis = (target − shoulder) / ‖…‖` inside `computeAdaptiveOrientation`.
- **D-02** (torso +Z up reference for roll stability) — Task 1 step 5: `KDL::Vector up(0.0, 0.0, 1.0)` is the primary reference.
- **D-03** (+Y fallback when near-parallel) — Task 1 step 5: switch to `KDL::Vector(0.0, 1.0, 0.0)` when `|dot(x_axis, up)| > kParallelDotThreshold`.
- **D-04** (single deterministic orientation, no multi-candidate fallback) — Task 1 step 5: one quaternion produced per goal; no candidate list, no IK retry over orientations.
- **D-05** (shoulder ref = `right_shoulder_pitch_link` origin) — Task 1 step 4: cache populated from FK on segment 1 of `kdl_chain_right`, which by construction is `right_shoulder_pitch_link`.
- **D-06** (compute via URDF/KDL, no runtime TF lookup) — Task 1 step 4: FK on cached `kdl_chain_right` at init; no `tf_buffer_.lookupTransform` is added by Phase 8.
- **D-07** (shoulder link name hardcoded, no new ROS param) — Task 1 step 5: `kMinTargetDistance` and `kParallelDotThreshold` are local `constexpr`, not ROS parameters; only `adaptive_orientation_enabled` is added.
- **D-08** (reject too-close target) — Task 1 step 6: `RCLCPP_ERROR ... within 0.05 m of right shoulder` then `return` with no trajectory publish (must_haves.truths #4).
- **D-09** (default overwrite of incoming orientation) — Task 1 step 6: when `adaptive_orientation_enabled_=true`, mutate `pose_in_base.pose.orientation.{x,y,z,w}` in place (must_haves.truths #3).
- **D-10** (`adaptive_orientation_enabled` ROS param, default `true`) — Task 1 step 3 (`declare_parameter("adaptive_orientation_enabled", true)`) + Task 2 (launch arg with `default_value='true'`).
- **D-11** (when `=false`, preserve old behavior exactly) — Task 1 step 6: the if-block is skipped entirely; downstream code consumes `pose_in_base.pose.orientation` unchanged. must_haves.truths #5 asserts byte-for-byte regression.
- **D-12** (per-goal INFO log: target xyz, shoulder xyz, direction, quaternion) — Task 1 step 6: `RCLCPP_INFO("Adaptive orientation: target=[…] shoulder=[…] dir=[…] q=[…]", …)` emitted exactly once per non-rejected goal.

Decisions D-13, D-14, D-15, D-16 (verification scope) are covered by Plan 02.
</decision_coverage>
