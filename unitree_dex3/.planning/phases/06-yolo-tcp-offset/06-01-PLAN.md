---
phase: 6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/unitree_g1_dex3_stack-main/CMakeLists.txt
  - src/unitree_g1_dex3_stack-main/package.xml
  - src/unitree_g1_dex3_stack-main/launch/reach.launch.py
autonomous: true
requirements:
  - CLEAN-01
---

# Plan 06-01: YOLO Cleanup

<objective>
Delete all YOLO-related files, the bboxes_ex_msgs package, and simplify reach.launch.py to remove perception pipeline references.
</objective>

<must_haves>
## Goal-Backward Verification
- truth: "src/bboxes_ex_msgs/ directory does not exist"
- truth: "ultralytics_detector.py, project_to_3d_node.cpp, detection_to_goal_node.cpp, visual_detection_yolo_tester.cpp do not exist"
- truth: "perception.launch.py, visual_detect_yolo.launch.py, elevator_perception.launch.py do not exist"
- truth: "best.pt and run_perception.sh do not exist in project root"
- truth: "CMakeLists.txt does not reference bboxes_ex_msgs, project_to_3d_node, detection_to_goal_node, or visual_detection_yolo_tester"
- truth: "package.xml does not contain bboxes_ex_msgs, image_transport, pcl_conversions, or pcl_msgs"
- truth: "reach.launch.py does not reference perception.launch.py, model_path, target_class, imshow, or keyboard_trigger_node"
- truth: "colcon build --packages-select unitree_g1_dex3_stack succeeds"
</must_haves>

<tasks>
## Tasks

### Task 1: Delete YOLO source files and bboxes_ex_msgs package
<read_first>
- src/unitree_g1_dex3_stack-main/scripts/ultralytics_detector.py
- src/unitree_g1_dex3_stack-main/src/project_to_3d_node.cpp
- src/unitree_g1_dex3_stack-main/src/detection_to_goal_node.cpp
- src/unitree_g1_dex3_stack-main/src/visual_detection_yolo_tester.cpp
</read_first>

<action>
Delete the following files and directories:
- src/unitree_g1_dex3_stack-main/scripts/ultralytics_detector.py
- src/unitree_g1_dex3_stack-main/scripts/elevator_ocr_node.py (depends on bboxes_ex_msgs which is being deleted)
- src/unitree_g1_dex3_stack-main/src/project_to_3d_node.cpp
- src/unitree_g1_dex3_stack-main/src/detection_to_goal_node.cpp
- src/unitree_g1_dex3_stack-main/src/visual_detection_yolo_tester.cpp
- src/unitree_g1_dex3_stack-main/launch/perception.launch.py
- src/unitree_g1_dex3_stack-main/launch/visual_detect_yolo.launch.py
- src/unitree_g1_dex3_stack-main/launch/elevator_perception.launch.py
- src/unitree_g1_dex3_stack-main/docs/ARCHITECTURE(yolo,failed).md
- src/bboxes_ex_msgs/ (entire directory)
- best.pt (project root)
- run_perception.sh (project root)
</action>

<acceptance_criteria>
- `ls src/unitree_g1_dex3_stack-main/scripts/ultralytics_detector.py` returns "No such file or directory"
- `ls src/unitree_g1_dex3_stack-main/scripts/elevator_ocr_node.py` returns "No such file or directory"
- `ls src/unitree_g1_dex3_stack-main/src/project_to_3d_node.cpp` returns "No such file or directory"
- `ls src/unitree_g1_dex3_stack-main/src/detection_to_goal_node.cpp` returns "No such file or directory"
- `ls src/unitree_g1_dex3_stack-main/src/visual_detection_yolo_tester.cpp` returns "No such file or directory"
- `ls src/unitree_g1_dex3_stack-main/launch/perception.launch.py` returns "No such file or directory"
- `ls src/unitree_g1_dex3_stack-main/launch/visual_detect_yolo.launch.py` returns "No such file or directory"
- `ls src/unitree_g1_dex3_stack-main/launch/elevator_perception.launch.py` returns "No such file or directory"
- `ls src/bboxes_ex_msgs` returns "No such file or directory"
- `ls best.pt` returns "No such file or directory"
- `ls run_perception.sh` returns "No such file or directory"
</acceptance_criteria>

### Task 2: Clean CMakeLists.txt
<read_first>
- src/unitree_g1_dex3_stack-main/CMakeLists.txt
</read_first>

<action>
In src/unitree_g1_dex3_stack-main/CMakeLists.txt:

1. Remove these find_package lines:
   - find_package(bboxes_ex_msgs REQUIRED)
   - find_package(image_transport REQUIRED)
   - find_package(pcl_conversions REQUIRED)
   - find_package(PCL REQUIRED)

2. Remove ${PCL_INCLUDE_DIRS} from include_directories() block.

3. Remove the add_executable lines for:
   - project_to_3d_node
   - detection_to_goal_node
   - visual_detection_yolo_tester

4. Remove the ament_target_dependencies blocks for:
   - project_to_3d_node (rclcpp vision_msgs bboxes_ex_msgs sensor_msgs geometry_msgs std_msgs cv_bridge image_transport message_filters pcl_conversions tf2 tf2_ros)
   - detection_to_goal_node (rclcpp vision_msgs bboxes_ex_msgs sensor_msgs geometry_msgs std_msgs)
   - visual_detection_yolo_tester (rclcpp vision_msgs geometry_msgs tf2 tf2_ros tf2_geometry_msgs)

5. Remove the target_link_libraries block for project_to_3d_node (${OpenCV_LIBRARIES} ${PCL_LIBRARIES}).

6. Remove project_to_3d_node, detection_to_goal_node, visual_detection_yolo_tester from install(TARGETS ...) list.

7. Remove scripts/ultralytics_detector.py and scripts/elevator_ocr_node.py from install(PROGRAMS ...) list.

Keep: visual_detection_tester target and its ament_target_dependencies. Keep OpenCV, cv_bridge, message_filters find_package. Keep ${OpenCV_INCLUDE_DIRS} in include_directories.
</action>

<acceptance_criteria>
- `grep -c "bboxes_ex_msgs" src/unitree_g1_dex3_stack-main/CMakeLists.txt` returns 0
- `grep -c "project_to_3d_node" src/unitree_g1_dex3_stack-main/CMakeLists.txt` returns 0
- `grep -c "detection_to_goal_node" src/unitree_g1_dex3_stack-main/CMakeLists.txt` returns 0
- `grep -c "visual_detection_yolo_tester" src/unitree_g1_dex3_stack-main/CMakeLists.txt` returns 0
- `grep -c "PCL" src/unitree_g1_dex3_stack-main/CMakeLists.txt` returns 0
- `grep -c "image_transport" src/unitree_g1_dex3_stack-main/CMakeLists.txt` returns 0
- `grep -c "pcl_conversions" src/unitree_g1_dex3_stack-main/CMakeLists.txt` returns 0
- `grep -c "ultralytics_detector" src/unitree_g1_dex3_stack-main/CMakeLists.txt` returns 0
- `grep -c "elevator_ocr_node" src/unitree_g1_dex3_stack-main/CMakeLists.txt` returns 0
- `grep "visual_detection_tester" src/unitree_g1_dex3_stack-main/CMakeLists.txt` returns at least one match (target kept)
- `grep "find_package(OpenCV" src/unitree_g1_dex3_stack-main/CMakeLists.txt` returns a match
</acceptance_criteria>

### Task 3: Clean package.xml
<read_first>
- src/unitree_g1_dex3_stack-main/package.xml
</read_first>

<action>
In src/unitree_g1_dex3_stack-main/package.xml, remove these four <depend> lines:
- <depend>bboxes_ex_msgs</depend>
- <depend>image_transport</depend>
- <depend>pcl_conversions</depend>
- <depend>pcl_msgs</depend>

Keep: <depend>vision_msgs</depend>, <depend>cv_bridge</depend>, <depend>message_filters</depend>.
</action>

<acceptance_criteria>
- `grep -c "bboxes_ex_msgs" src/unitree_g1_dex3_stack-main/package.xml` returns 0
- `grep -c "image_transport" src/unitree_g1_dex3_stack-main/package.xml` returns 0
- `grep -c "pcl_conversions" src/unitree_g1_dex3_stack-main/package.xml` returns 0
- `grep -c "pcl_msgs" src/unitree_g1_dex3_stack-main/package.xml` returns 0
- `grep "vision_msgs" src/unitree_g1_dex3_stack-main/package.xml` returns a match
- `grep "cv_bridge" src/unitree_g1_dex3_stack-main/package.xml` returns a match
</acceptance_criteria>

### Task 4: Simplify reach.launch.py
<read_first>
- src/unitree_g1_dex3_stack-main/launch/reach.launch.py
</read_first>

<action>
Rewrite src/unitree_g1_dex3_stack-main/launch/reach.launch.py to contain only:

1. Imports: os, launch (LaunchDescription, DeclareLaunchArgument, LaunchConfiguration), launch.actions (TimerAction), launch_ros.actions (Node), launch.actions (IncludeLaunchDescription), PythonLaunchDescriptionSource.

2. DeclareLaunchArgument for planning_timeout (default '1.0').

3. robot_launch: IncludeLaunchDescription of robot.launch.py (no launch_arguments needed).

4. d435_tf_node: Node with package='tf2_ros', executable='static_transform_publisher' — keep the exact same arguments as current (the static TF for d435_link → camera_link).

5. planner_launch: IncludeLaunchDescription of planner.launch.py with launch_arguments={'planning_timeout': LaunchConfiguration('planning_timeout')}.

6. control_launch: IncludeLaunchDescription of control.launch.py.

7. delayed_actions: TimerAction(period=3.0, actions=[planner_launch, control_launch]).

8. Return: [planning_timeout_arg, robot_launch, d435_tf_node, delayed_actions].

Remove: model_path_arg, target_class_arg, imshow_arg, perception_launch, keyboard_node. Do NOT include perception.launch.py. Do NOT reference model_path, target_class, imshow, keyboard_trigger_node, or allowed_classes.
</action>

<acceptance_criteria>
- `grep -c "perception" src/unitree_g1_dex3_stack-main/launch/reach.launch.py` returns 0
- `grep -c "model_path" src/unitree_g1_dex3_stack-main/launch/reach.launch.py` returns 0
- `grep -c "target_class" src/unitree_g1_dex3_stack-main/launch/reach.launch.py` returns 0
- `grep -c "imshow" src/unitree_g1_dex3_stack-main/launch/reach.launch.py` returns 0
- `grep -c "keyboard_trigger" src/unitree_g1_dex3_stack-main/launch/reach.launch.py` returns 0
- `grep "planning_timeout" src/unitree_g1_dex3_stack-main/launch/reach.launch.py` returns at least one match
- `grep "robot.launch.py" src/unitree_g1_dex3_stack-main/launch/reach.launch.py` returns a match
- `grep "planner.launch.py" src/unitree_g1_dex3_stack-main/launch/reach.launch.py` returns a match
- `grep "control.launch.py" src/unitree_g1_dex3_stack-main/launch/reach.launch.py` returns a match
- `grep "static_transform_publisher" src/unitree_g1_dex3_stack-main/launch/reach.launch.py` returns a match
- `grep "TimerAction" src/unitree_g1_dex3_stack-main/launch/reach.launch.py` returns a match
</acceptance_criteria>

### Task 5: Build verification
<read_first>
- src/unitree_g1_dex3_stack-main/CMakeLists.txt
</read_first>

<action>
Run `colcon build --packages-select unitree_g1_dex3_stack` from the workspace root. Verify it completes with 0 errors. If bboxes_ex_msgs is still referenced anywhere in the workspace (e.g., colcon tries to resolve it), also verify that `colcon build --packages-up-to unitree_g1_dex3_stack` succeeds (since bboxes_ex_msgs package directory is deleted, it should no longer be discovered).
</action>

<acceptance_criteria>
- `colcon build --packages-select unitree_g1_dex3_stack` exits with return code 0
- Build output contains "1 package finished" and no "failed" messages
- No compiler errors referencing bboxes_ex_msgs, project_to_3d_node, detection_to_goal_node, or visual_detection_yolo_tester
</acceptance_criteria>
</tasks>

<verification>
## Verification
- Run `colcon build --packages-select unitree_g1_dex3_stack` — must succeed
- Run `grep -r "bboxes_ex_msgs" src/` — must return no matches
- Run `grep -r "ultralytics" src/` — must return no matches
- Confirm `src/unitree_g1_dex3_stack-main/src/visual_detection_tester.cpp` still exists (kept per D-02)
- Confirm `src/unitree_g1_dex3_stack-main/launch/visual_detect_click.launch.py` still exists (kept per D-02)
</verification>

<success_criteria>
## Success Criteria
- All YOLO-related files deleted (12+ files/directories)
- CMakeLists.txt and package.xml compile cleanly without deleted dependencies
- reach.launch.py contains only robot + d435_tf + planner + control
- colcon build succeeds with no errors
</success_criteria>
