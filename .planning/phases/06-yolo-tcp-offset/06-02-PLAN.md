---
phase: 6
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/unitree_g1_dex3_stack-main/robots/g1_description/g1_29dof_lock_waist_with_hand_rev_1_0.urdf
  - src/unitree_g1_dex3_stack-main/robots/g1_description/g1_29dof_lock_waist_with_hand_rev_1_0_collision_primitives.urdf
autonomous: true
requirements:
  - TCP-01
---

# Plan 06-02: URDF TCP Link

<objective>
Add a virtual fixed link `right_tcp_link` to both URDF files, extending the kinematic chain 0.175m along the wrist_yaw X axis.
</objective>

<must_haves>
## Goal-Backward Verification
- truth: "Both URDF files contain a joint named right_tcp_joint of type fixed with parent right_wrist_yaw_link and child right_tcp_link"
- truth: "right_tcp_joint origin is xyz=0.175 0 0 rpy=0 0 0"
- truth: "right_tcp_link is defined as an empty link (no visual, collision, or inertial)"
- truth: "right_tcp_joint appears before right_hand_palm_joint in both files"
</must_haves>

<tasks>
## Tasks

### Task 1: Add right_tcp_link to default URDF
<read_first>
- src/unitree_g1_dex3_stack-main/robots/g1_description/g1_29dof_lock_waist_with_hand_rev_1_0.urdf
</read_first>

<action>
In src/unitree_g1_dex3_stack-main/robots/g1_description/g1_29dof_lock_waist_with_hand_rev_1_0.urdf, insert the following XML immediately before the line `<joint name="right_hand_palm_joint" type="fixed">`:

A comment line: `<!-- TCP (Tool Center Point) virtual link: 0.175m along wrist_yaw X axis -->`
A fixed joint named `right_tcp_joint` with:
- origin xyz="0.175 0 0" rpy="0 0 0"
- parent link="right_wrist_yaw_link"
- child link="right_tcp_link"

An empty link element: `<link name="right_tcp_link" />`

This makes right_tcp_link a sibling of right_hand_palm_link (both children of right_wrist_yaw_link).
</action>

<acceptance_criteria>
- `grep "right_tcp_joint" src/unitree_g1_dex3_stack-main/robots/g1_description/g1_29dof_lock_waist_with_hand_rev_1_0.urdf` returns a match
- `grep "right_tcp_link" src/unitree_g1_dex3_stack-main/robots/g1_description/g1_29dof_lock_waist_with_hand_rev_1_0.urdf` returns at least 2 matches (joint child + link element)
- `grep -A2 "right_tcp_joint" src/unitree_g1_dex3_stack-main/robots/g1_description/g1_29dof_lock_waist_with_hand_rev_1_0.urdf` contains `xyz="0.175 0 0"`
- `grep -n "right_tcp_joint" ... | head -1` line number is less than `grep -n "right_hand_palm_joint" ... | head -1` line number (tcp_joint appears first)
</acceptance_criteria>

### Task 2: Add right_tcp_link to collision primitives URDF
<read_first>
- src/unitree_g1_dex3_stack-main/robots/g1_description/g1_29dof_lock_waist_with_hand_rev_1_0_collision_primitives.urdf
</read_first>

<action>
In src/unitree_g1_dex3_stack-main/robots/g1_description/g1_29dof_lock_waist_with_hand_rev_1_0_collision_primitives.urdf, insert the identical XML block immediately before the line `<joint name="right_hand_palm_joint" type="fixed">`:

A comment line: `<!-- TCP (Tool Center Point) virtual link: 0.175m along wrist_yaw X axis -->`
A fixed joint named `right_tcp_joint` with:
- origin xyz="0.175 0 0" rpy="0 0 0"
- parent link="right_wrist_yaw_link"
- child link="right_tcp_link"

An empty link element: `<link name="right_tcp_link" />`
</action>

<acceptance_criteria>
- `grep "right_tcp_joint" src/unitree_g1_dex3_stack-main/robots/g1_description/g1_29dof_lock_waist_with_hand_rev_1_0_collision_primitives.urdf` returns a match
- `grep "right_tcp_link" src/unitree_g1_dex3_stack-main/robots/g1_description/g1_29dof_lock_waist_with_hand_rev_1_0_collision_primitives.urdf` returns at least 2 matches
- `grep -A2 "right_tcp_joint" src/unitree_g1_dex3_stack-main/robots/g1_description/g1_29dof_lock_waist_with_hand_rev_1_0_collision_primitives.urdf` contains `xyz="0.175 0 0"`
- `grep -n "right_tcp_joint" ... | head -1` line number is less than `grep -n "right_hand_palm_joint" ... | head -1` line number
</acceptance_criteria>

### Task 3: Validate URDF parse
<read_first>
- src/unitree_g1_dex3_stack-main/robots/g1_description/g1_29dof_lock_waist_with_hand_rev_1_0.urdf
</read_first>

<action>
Run `check_urdf` on both URDF files to verify they parse without errors. If `check_urdf` is not available, use `python3 -c "import xml.etree.ElementTree as ET; ET.parse('path/to/file.urdf')"` to verify XML well-formedness. Additionally verify that `right_tcp_link` appears in the parsed tree as a child of `right_wrist_yaw_link` via the `right_tcp_joint`.
</action>

<acceptance_criteria>
- XML parse of g1_29dof_lock_waist_with_hand_rev_1_0.urdf succeeds without exceptions
- XML parse of g1_29dof_lock_waist_with_hand_rev_1_0_collision_primitives.urdf succeeds without exceptions
- Both files contain exactly one occurrence of `<joint name="right_tcp_joint"`
- Both files contain exactly one occurrence of `<link name="right_tcp_link"`
</acceptance_criteria>
</tasks>

<verification>
## Verification
- XML parse both URDF files — no errors
- grep confirms right_tcp_joint and right_tcp_link present in both files
- grep confirms origin xyz="0.175 0 0" in both right_tcp_joint definitions
- Verify right_tcp_joint appears before right_hand_palm_joint in file order
</verification>

<success_criteria>
## Success Criteria
- Both URDF files contain right_tcp_link as a fixed child of right_wrist_yaw_link at 0.175m X offset
- URDF files are valid XML
- No existing links or joints are modified
</success_criteria>
